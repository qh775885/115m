# 当前迭代机会

> 已完成 P1-P8（媒体墙适配/滚动/快捷键/解压/背景/overlay/路径/缩略图缓存），详见提交历史。
> 此文件仅保留当前可迭代项。

## 当前可迭代

_暂无。若有新发现或新需求，再行补充。_

## 不做（收益低或已固化）

- `src/lib/pro-api.ts:33` 依赖对象顺序取结果——调用方单 pickcode，风险可忽略。
- `src/player/core/overlay.ts` 面包屑 innerHTML——已 escapeHtml 防 XSS，可接受。

## 备注

- **视频缩略图风控**：若有人反馈风控，源头是快速滚动触发大量抽帧请求（getM3u8 + 分片）。可考虑"滚动抽帧限流"作为独立迭代项，当前未遇风控暂不处理。
