# 当前迭代机会

> 已经整体审视后确认、可立即启动的项目。按优先级排列。
> 每项标注：位置、问题、改动量、风险。改动量分 S（小）/M（中）/L（大）。

## 已确认优先级

### P1. 媒体墙选择器/逻辑优化（进行中）

- **位置**：`src/content/core/media-wall*.ts`、`src/content/core/home-scroll-binder.ts`、`src/content/core/scroll-history.ts`
- **内容**：
  - 媒体墙构建/刷新时重复 DOM 扫描、无防抖重渲染
  - `home-scroll-binder.ts`：每个 DOM mutation 执行 2 次全量 `buildListKey`，无防抖 → 列表大时性能隐患
  - `scroll-history.ts`：store 全量读写、key 含内容指纹可能导致滚动位置被异常重置、无条数上限
- **改动量**：M（适配层已就位，选择器收敛已完成，本项是逻辑层优化）
- **风险**：低，涉及渲染/滚动行为，需页面实测

### P2. 键盘快捷键劫持修复（最高风险边界 bug）

- **位置**：`src/player/core/keyboard.ts:30`
- **问题**：`keydown` 监听不检查 `e.target`，焦点在输入框时按空格/方向键会被劫持（无法输入空格）；且与 Artplayer 默认 `hotkey: true` 冲突可能双触发（空格双 toggle、方向键双 seek）。
- **改动量**：S
- **风险**：中——涉及播放器交互，必须实测播放页输入框与快捷键

### P3. 压缩包工具函数合并

- **位置**：`unarchive-helper.ts:1-18` 与 `unarchive-actions.ts:5-41`
- **问题**：压缩包扩展名三件套双份实现且**行为不一致**（helper 不处理 `tar.gz` 复合扩展名）。
- **改动量**：S（抽 `src/shared/archive.ts`，两处引用）
- **风险**：中——需确认 `tar.gz` 剥离行为统一后的解压结果一致

### P4. 解压相关逻辑收紧

- **位置**：
  - `unarchive-actions.ts:43-46` `isSecondaryVolume` 误判任意 `文件名.三位数字` 为分卷（如 `photo.042` 被静默跳过）
  - `unarchive-actions.ts:501-507` 四事件无防抖全量扫描
  - `unarchive-helper.ts:205-210` 固定延迟轮询无清理、无事件驱动
- **改动量**：M
- **风险**：中——解压是核心功能，需逐项实测

### P5. 背景脚本与杂项清理

- **位置**：`src/background/index.ts`
  - PING 死分支（L115-117 不可达）
  - 空 `onInstalled` 回调
  - `assertTrustedSender` 在每个 case 重复，可提炼
  - `chrome.downloads.download` 未 await/catch，失败仍返回 success
- **改动量**：S-M
- **风险**：低

### P6. 播放器 overlay 小优化

- **位置**：`src/player/core/overlay.ts`
  - `escapeHtml`/`esc` 双名（L2/L809）
  - L287 两处文案仅差一字，疑似笔误
  - L795 `handlePlaylistToggle` 无 try-catch
- **改动量**：S
- **风险**：低

### P7. 打开播放器路径统一

- **位置**：`src/content/core/action-buttons.ts:24-25`
- **问题**：硬编码 `https://115vod.com/...`（绕过 `VOD_URL` 常量），且直接 `window.open` 绕过 `OPEN_TAB` 去重通道，与 `player-open.ts` 行为不一致。
- **改动量**：S
- **风险**：中——需确认 115vod 直开是否为刻意行为

### P8. 视频缩略图缓存策略

- **位置**：`src/lib/videoThumbnail.ts`
  - 模块级 Map 缓存无上限/清理
  - L365 去重 key 的 `imgUrl.slice(0,32)` 区分度低（data URL 前缀固定）
  - L447/L518 `startedAt` 死代码
  - 全量写 chrome.storage 无去重
- **改动量**：M
- **风险**：中——涉及缓存正确性，改动后需验证封面命中率

## 不做（收益低或已固化）

- `src/lib/pro-api.ts:33` 依赖对象顺序取结果——调用方单 pickcode，风险可忽略。
- `src/player/core/overlay.ts` 面包屑 innerHTML——已 escapeHtml 防 XSS，可接受。
