# 当前迭代机会

> 已经整体审视后确认、可立即启动的项目。按优先级排列。
> 每项标注：位置、问题、改动量、风险。改动量分 S（小）/M（中）/L（大）。

## 已确认优先级

### P1. 媒体墙选择器/逻辑优化（✅ 已完成 v1.8.3 ~ v1.8.5）

- **完成内容**：
  - 适配层收敛：新建 `native-dom.ts` 集中管理 115 原生选择器（v1.8.3）
  - `home-scroll-binder.ts`：MutationObserver 触发加 120ms 防抖（v1.8.4）
  - `scroll-history.ts`：key 改为结构标识（cid+offset+tpl）、store 加 200 条上限（v1.8.4）
  - `media-wall.ts`：列表元素引用未变化时跳过重算/重渲染，force 参数保证星标/备注刷新（v1.8.5）
- **测试**：`native-dom.test.ts`（28 用例）、`scroll-history.test.ts`（10 用例）、`media-wall.test.ts`（5 用例）
- **遗留说明**：媒体墙自渲染仍会触发 `home.ts` 的 observer（渲染→扫描），但已由快速短路吸收，无循环重建

### P2. 键盘快捷键劫持修复（✅ 已完成）

- **位置**：`src/player/core/keyboard.ts`、`src/player/player.ts`
- **改动**：
  - `keyboard.ts`：`keydown` 跳过可编辑元素（input/textarea/select/contenteditable），输入框内不再劫持空格/方向键
  - `player.ts`：Artplayer 显式 `hotkey: false`，自定义快捷键成为唯一来源，避免双触发
- **测试**：`keyboard.test.ts`（6 用例）
- **风险**：中——涉及播放器交互

### P3. 压缩包工具函数合并（✅ 已完成）

- **位置**：新建 `src/shared/archive.ts`，`unarchive-helper.ts`、`unarchive-actions.ts` 引用共享实现
- **修复**：消除双份实现；`stripArchiveExtension` 统一处理复合扩展名（`a.tar.gz` 剥离为 `a`，原 helper 只剥一层得 `a.tar`）
- **测试**：`archive.test.ts`（6 用例）
- **风险**：中——已确认两处调用点行为统一

### P4. 解压相关逻辑收紧（✅ 已完成）

- **修复**：
  - `isSecondaryVolume` 移入 `shared/archive.ts`：不再误判 `photo.042` 类文件为分卷，仅识别 `xxx.partN.rar` 非首卷
  - 批量按钮可见性更新加 300ms 防抖（原四事件 + observer 无防抖全量扫描）
  - "新建文件夹"对话框增强改为 MutationObserver 事件驱动 + 5s 自清理（原固定延迟轮询，对话框晚出失效、连点堆积）
- **测试**：`archive.test.ts` 增至 10 用例

### P5. 背景脚本与杂项清理（✅ 已完成）

- **修复**：
  - 删除不可达的 PING 死分支（顶层已拦截）
  - 删除空 `onInstalled` 回调
  - `assertTrustedSender` 提炼到 switch 前统一校验，每个 case 不再重复
  - `chrome.downloads.download` 补 await + catch，失败返回 `{ success: false, error }`；`messages.ts` 新增 `RuntimeDownloadResponse` 类型

### P6. 播放器 overlay 小优化（✅ 已完成）

- **修复**：
  - 删除 `escapeHtml`/`esc` 双名别名
  - 播放结束面板文案统一为「当前视频播放完成」（原两分支仅差一字）
  - `handlePlaylistToggle` 补 try-catch，失败不再产生 unhandled rejection

### P7. 打开播放器路径统一（✅ 已完成）

- **改动**：`action-buttons.ts` 的「115播放」按钮改用 `VOD_URL` 常量（行为不变）
- **结论**：`window.open` 是刻意行为——该按钮打开 115 官方原生播放器，与 m115 自定义播放器（OPEN_TAB）是两个功能，不需统一

### P8. 视频缩略图缓存策略

- **位置**：`src/lib/videoThumbnail.ts`
  - 模块级 Map 缓存无上限/清理
  - L365 去重 key 的 `imgUrl.slice(0,32)` 区分度低（data URL 前缀固定）
  - L447/L518 `startedAt` 死代码
  - 全量写 chrome.storage 无去重
- **改动量**：M
- **风险**：中——涉及缓存正确性，改动后需验证封面命中率

## 不做（收益低或已固化）

- `src/lib/pro-api.ts:33` 依赖对象顺序取结果——调用方单 pickcode，风险可忽略。
- `src/player/core/overlay.ts` 面包屑 innerHTML——已 escapeHtml 防 XSS，可接受。
