# 115m 2.0 播放器架构迁移计划

> 本文档实时追踪 Vidstack 2.0 现代工业级播放器迁移进度。每推进一步，均在此标记并对齐验收。

---

## 进度仪表盘

| 阶段 | 任务目标 | 状态 | 交付与验收依据 |
| :--- | :--- | :---: | :--- |
| **阶段 0** | 物理隔离阵地与代码基线 | **已完成** | `115m-v2` 独立目录就绪，依赖重置，501 测试全绿 |
| **阶段 1** | 底座点火与流媒体纯管道 | **已完成** | 纯净原生播放器，33G MKV 与 MP4 秒开有画有声 |
| **阶段 2** | 架构设计与 2.0 纯 UI 壳子 | **进行中** | Apple TV 级极简流光 UI 骨架敲定，纯交互动效 |
| **阶段 3** | 115 业务功能逐项单线串联 | 待开始 | 音轨/字幕/清晰度/文件管理/快捷键/历史逐一打通 |
| **阶段 4** | 极限实战看剧测试与合流 | 待开始 | 真实日常重度看剧零暗雷，合入主线正式发布 2.0 |

---

## 阶段 0：物理隔离阵地与纯净基线（已完成）

- [x] **0.1 物理隔离**：独立创建 `E:\qh775885\115m-v2` 开发目录，杜绝污染主线。
- [x] **0.2 独立分支**：检出 `feat/v2-vidstack-player` 支线，远端对齐 GitHub 官方。
- [x] **0.3 核心依赖**：安装稳定版 `vidstack@1.15.6`。
- [x] **0.4 基线全绿**：初始化 WXT 构建配置，完成 501 个基线自动化测试验证。

---

## 阶段 1：底座点火与流媒体纯管道（已完成）

> **原则**：绝不搞任何花哨 UI，绝不加复杂菜单，只验证播放内核能否在 115 环境下顺畅跑通。

- [x] **1.1 Chrome 扩展构建适配**：
  - 静态内联注册 Vidstack Web Components 与 Hls 实例，彻底消除 MV3 动态 import 报错风险。
- [x] **1.2 极简纯净点火页面**：
  - 构造只包含 `<media-player>` 与 `<media-provider>` 的纯净容器，左上角挂载状态指示胶囊。
- [x] **1.3 输油管道 1：Cookie 鉴权与跨域透传**：
  - 挂接 `xhr.withCredentials = true`，确保 115 CDN（`*.115cdn.net` / `115vod.com`）鉴权通行。
- [x] **1.4 输油管道 2：音视频分离与 Master M3U8 合成**：
  - 封装 `buildMasterHlsBlobUrl`，自动合成带有音频轨道的 Master Playlist Blob，根治 33G MKV 音画分离黑屏。
- [x] **1.5 编译打包与实测验收**：
  - 零亲自真机实测验证通过：左上角状态徽章点亮，33G MKV《色戒》及普通 MP4 秒开有画有声、拖动丝滑，**底座 100% 就绪！**

---

## 阶段 2：架构设计与 2.0 纯 UI 壳子（进行中）

> **原则**：只做视觉、结构与交互动效，不挂接后台业务，保持逻辑轻盈。

- [x] **2.0 功能盘点与架构规划落档**：
  - 彻底梳理旧版 22 项全量功能并建立新 UI 插槽映射图谱；
  - 确立四层微内核解耦架构（Kernel / View / State / Adapters），从物理结构上杜绝巨石代码。
- [ ] **2.1 极光 UI 骨架与插槽树搭建**：
  - `aurora-topbar`（顶部沉浸式遮罩、标题、文件元数据、快捷操作插槽）；
  - `aurora-center`（居中悬浮极光流光主控胶囊：上一集 / 大播放暂停 / 下一集）；
  - `aurora-timeline`（贴底 2px 纤细流光进度条，Hover 展开为 6px 极光青蓝渐变，带玻璃拟态时间提示气泡）；
  - `aurora-controls`（底部轻悬浮控制栏：等宽时间码、横向呼吸水银音量滑块、右侧高频菜单入口）；
  - `aurora-drawer`（右侧无感毛玻璃抽屉式选集面板，不挤压视频画面）；
  - `aurora-sheet`（向上平滑升起的圆角毛玻璃浮层，用于画质/音轨/字幕/倍速等菜单）。
- [ ] **2.2 视觉与交互动效调优**：
  - 自动淡出/淡入缓动控制（鼠标停顿 2.5 秒平滑淡出，轻微移动自然浮现）；
  - 纯前端状态切换与交互手感调至 Apple TV / Netflix 工业水准。
- [ ] **2.3 零业务真机 UI 质感验收**：
  - 由零亲自在浏览器中审阅静态与动态视觉效果，满意后再行接入真实业务。

---

## 旧版 22 项功能资产与 2.0 映射对准图谱

| 序号 | 功能板块 | 旧版功能明细（1.9.5） | 2.0 对应新 UI 插槽 / 交互形式 |
| :---: | :--- | :--- | :--- |
| **1** | 顶部状态栏 | 退出 / 返回视频列表 | `aurora-topbar` 左侧微透光圆形退出按钮 |
| **2** | 顶部状态栏 | 视频标题与文件名展示 | `aurora-topbar` 居中左侧主标题（单行防溢出） |
| **3** | 顶部状态栏 | 文件元数据徽章（大小/分辨率/格式） | `aurora-topbar` 标题后跟的流光微徽章（如 `33.2 GB` `4K` `MKV`） |
| **4** | 顶部状态栏 | 快捷下载原画视频按钮 | `aurora-topbar` 右侧快捷操作按钮区 |
| **5** | 顶部状态栏 | 移动文件到目录 / 收藏至星标 | `aurora-topbar` 右侧快捷操作按钮区 |
| **6** | 居中主控 | 播放上一集（列表前一个视频） | `aurora-center` 居中胶囊左侧按钮（带键盘 `[` 快捷提示） |
| **7** | 居中主控 | 居中主控播放 / 暂停切换 | `aurora-center` 居中胶囊中央大按钮（带脉冲微光） |
| **8** | 居中主控 | 播放下一集（列表后一个视频） | `aurora-center` 居中胶囊右侧按钮（带键盘 `]` 快捷提示） |
| **9** | 底部左侧簇 | 底部微缩播放 / 暂停按键 | `aurora-controls` 左侧常规播放控制 |
| **10** | 底部左侧簇 | 当前时间码 / 视频总时长 | `aurora-controls` 紧凑等宽数字时间显示（`00:12:45 / 02:38:14`） |
| **11** | 底部左侧簇 | 音量图标与横向滑杆调节 | `aurora-controls` 横向水银微动效展开音量胶囊（静音一键切换） |
| **12** | 极光时间轴 | 缓冲进度与播放进度平滑追踪 | `aurora-timeline` 贴底 2px 静默线，Hover 呼吸展为 6px 极光流光带 |
| **13** | 极光时间轴 | 时间轴鼠标悬浮时间码微气泡 | `aurora-timeline` 悬浮跟随玻璃拟态流光气泡 |
| **14** | 极光时间轴 | 拖拽进度条 Seek 吸附 | `aurora-timeline` 丝滑无级拖拽，松手即刻响应 |
| **15** | 底部右侧簇 | 画质切换（原画直链 / 超清 / 高清等） | `aurora-controls` 右侧画质胶囊，点击自下而上滑出 `aurora-sheet` |
| **16** | 底部右侧簇 | 多音频轨道识别与切换（国语/多语） | `aurora-controls` 右侧音轨胶囊，呼出 `aurora-sheet` |
| **17** | 底部右侧簇 | 内置/外挂字幕（加载/偏移/样式） | `aurora-controls` 右侧字幕胶囊，呼出 `aurora-sheet` |
| **18** | 底部右侧簇 | 倍速播放调节（0.5x ~ 3.0x） | `aurora-controls` 右侧倍速胶囊，呼出 `aurora-sheet` |
| **19** | 底部右侧簇 | 画面旋转（90° 顺时针旋转 / 镜像） | `aurora-controls` 右侧更多设置项 |
| **20** | 底部右侧簇 | 播放模式（顺序播放 / 单集循环 / 列表循环）| `aurora-controls` 循环模式快速切换图标 |
| **21** | 底部右侧簇 | 画中画 (PiP) / 网页全屏 / 原生全屏 | `aurora-controls` 最右侧全屏与视窗控制群 |
| **22** | 侧边选集抽屉| 侧边栏选集列表（高亮当前集、快速跳播） | `aurora-drawer` 右侧无感毛玻璃滑入浮层（绝不挤压变形视频） |

---

## 2.0 现代微内核解耦架构规约

为了彻底告别 1.9.5 将千行逻辑揉杂在单个 `player.ts` 中的历史包袱，2.0 实行**四层单向流解耦架构**：

```
src/player/v2/
├── kernel/            # Layer 1: 纯流媒体播放核心
│   ├── player-core.ts # 包装 Vidstack 播放器，提供标准播放、暂停、Seek 接口
│   └── stream-hls.ts  # Hls 引擎生命周期、Cookie 凭证与 Master M3U8 合成
│
├── ui/                # Layer 2: 极光 UI 视图层（纯组件化，零业务污染）
│   ├── topbar.ts      # 顶部沉浸遮罩与标题/元数据/快捷按钮
│   ├── center.ts      # 居中悬浮极光流光主控胶囊
│   ├── timeline.ts    # 贴底 2px 极光流光纤细时间轴
│   ├── controls.ts    # 底部控制栏与横向呼吸音量胶囊
│   ├── drawer.ts      # 右侧无感选集抽屉
│   ├── sheet.ts       # 向上滑出的玻璃拟态微卡片面板
│   └── theme.css      # 2.0 极光深空专属流光样式变量系统
│
├── state/             # Layer 3: 响应式状态管理（单向数据流）
│   ├── player-state.ts# 纯状态树（当前时间、时长、音量、选集中、菜单激活态）
│   └── store.ts       # 轻量极简事件发布订阅中心
│
└── adapters/          # Layer 4: 115 业务与外部特权适配层
    ├── bridge.ts      # 主世界与 Chrome 扩展沙盒的通信隧道
    ├── services.ts    # 115 视频解析、转码探测与直链获取
    ├── keyboard.ts    # 全局键盘事件映射（空格、步进、音量、全屏）
    └── history.ts     # 本地观看历史进度防抖持久化
```

**解耦四大纪律**：
1. **UI 不直接碰 115 接口**：UI 组件只监听 `state` 并触发 `actions`，绝不直接发起网络请求；
2. **播放核心不依赖 UI**：`kernel` 可以在没有任何 UI 的情况下独立播放流媒体；
3. **单文件单一职责**：单个 UI 组件代码量严格控制在 150 行以内，杜绝巨石文件；
4. **渐进装配**：阶段 2 只装配 `ui/` 与 `state/`，用 Mock 假数据把视觉与动效调到极致，验证合格后再接入 `adapters/`。

---

## 阶段 3：115 业务功能逐项单线串联（待开始）

> **原则**：像插积木一样单线接入，接一个验证一个，绝不混杂。

- [ ] **3.1 清晰度切换**：原画直链、超清、高清、标清平滑切换。
- [ ] **3.2 多音轨与音频控制**：国语/粤语/英语等多音频轨道识别与切换。
- [ ] **3.3 外挂与内置字幕**：115 字幕加载、偏移调节、样式渲染。
- [ ] **3.4 键盘全局快捷键**：空格暂停、左右快进/快退、上下音量、F 全屏、M 静音。
- [ ] **3.5 播放列表与选集**：侧边选集滑出、自动播放下一集。
- [ ] **3.6 网盘原生文件操作**：移动目录、重命名、删除、下载。
- [ ] **3.7 播放历史与进度记忆**：断点防抖记忆与进度恢复。

---

## 阶段 4：极限实战看剧测试与合流（待开始）

- [ ] **4.1 自动化测试对齐**：补充 2.0 核心模块的单元测试，保持全绿。
- [ ] **4.2 真实日常重度看剧**：在日常高频看剧中挑刺优化，确保绝对稳健。
- [ ] **4.3 合并主线**：确认零缺陷后，正式合并进 `main` 分支，发布 115m 2.0 正式版。
