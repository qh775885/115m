# 115m 迭代规划路线图

---

## 核心基调与战略定位

**基调定义**：**双轨驱动 · 现代化工业底座重铸 · 极简轻快实用**。

1. **主线（Master）绝对稳定**：
   - 保持现有稳定版本的轻量维护，禁止在现有播放器（Artplayer）脆弱的 CSS/DOM 上做任何缝合修补。
   - 用户日常使用与 Bug 修复不受任何大改影响，基线测试与打包必须始终全绿。

2. **支线（Feature Branch）独立演进**：
   - 全新播放器及后续多媒体体系的大改，必须严格在独立实验分支（`feat/vidstack-player`）中单向演进。
   - 拒绝闭门造车与手撕轮子，技术升级全部基于行业经过数千万次验证的现代工业级开源成熟方案（播放器选 **Vidstack**，看图器选 **PhotoSwipe**）。

3. **实用轻快优先，严守原生安全边界**：
   - **拒绝花哨无用的视觉噱头**（氛围光晕、GPU 画质着色器滤镜等一律剔除）。
   - **大图查看器**：追求极致轻快，核心是秒开、切图顺畅、无级手势与缩放跟手，不堆冗余功能。
   - **媒体墙与文件夹封面**：**严守原生优先与零风控铁律**，100% 沿用 115 原生封面与图片缩略图，不发起任何未授权第三方请求；仅做现代简约排版，提升辨识度。
   - **风控防护**：在左侧栏既有设置面板中预留「列表封面预览总开关」设计备选，保障大容量用户自由控权。

---

## 阶段一：视频播放器重铸（Vidstack 独立支线推进）

### Phase 0：实验分支与独立沙盒搭建（骨架验证）
- **目标**：在不污染主线代码的前提下，验证 Vidstack 在 Chrome Extension (WXT + Vite) 环境下的加载与打包可行性。
- **任务项**：
  1. 切出独立实验分支：`feat/vidstack-player`。
  2. 引入 Vidstack 及官方 Default Layout，在独立沙盒目录中构建极简测试播放页。
  3. 验证 WXT 构建兼容性（MV3 Content Script / 注入沙盒下的 Web Components 表现）。
  4. 使用测试流验证原生手感（拖拽微动效、响应式折叠、弹层阻尼）。

### Phase 1：操控习惯与工业级交互对齐（基础体验）
- **目标**：不碰复杂业务，把用户核心使用习惯完美还原进 Vidstack 架构中。
- **任务项**：
  1. **居中主控簇**：利用组件插槽规范构建「上一集 / 中央播放暂停 / 下一集」正中布局，摒弃一切暴力搬运 DOM 的陈旧脚本。
  2. **横向展开音量胶囊**：采用 Vidstack 官方原生横向滑块组件，实现丝滑顺畅的展开收起交互。
  3. **标准时间与进度轴**：时间码清晰分层显示，进度条悬浮微缩与拖拽吸附。
  4. **快捷键与全屏体系**：键盘全局按键映射（空格暂停、左右步进、上下音量、F 全屏）。

### Phase 2：115 专属业务管道平移（血肉融合）
- **目标**：将 115 独有的鉴权、选集与多清晰度逻辑无损移植入新底座。
- **任务项**：
  1. **原画与 HLS 混播管线**：
     - 原画直链（HTTP）直传播放。
     - 转码多码率（HLS m3u8）接驳 Vidstack 内置 HLS 引擎。
     - 自动渲染画质切换菜单（原画、超清、高清等）。
  2. **播放状态机与历史记录**：
     - 播放进度秒级回存至 115 本地/云端历史。
     - 载入时精准断点续播。
  3. **业务图层（Overlay）外挂集成**：
     - 顶部文件信息导航栏（文件名、体积、所在路径、收藏、移动）。
     - 右侧抽屉式播放列表侧边栏（平移现有选集交互与缩略图）。
     - 画面旋转控制。

### Phase 3：实测验收与灰度双轨合流（平稳着陆）
- **目标**：完成真实场景全功能验证，确保体验全方位超越老播放器后再考虑并入主线。
- **任务项**：
  1. **真实场景长视频/连播压测**：测试大文件、多集连续跳转、音轨切流时的内存与稳定性。
  2. **双播放器无缝切换机制**：在设置项中保留降级开关（可一键切换回经典播放器）。
  3. **测试覆盖补全**：为新控制器补充单元测试，确保达到项目既有健康基线。
  4. **主线合并审批**：经零亲自验收打分满意后，方可合并至主分支。

---

## 阶段二：全媒体套件生态升级规划（后置推进）

待视频播放器彻底稳定合流后，按部就班推进：

### 1. 大图查看器轻量现代化重构（PhotoSwipe v5）
- **核心诉求**：极致轻快、纯粹。
- **任务项**：
  - 彻底淘汰手写阻尼矩阵数学公式（`viewer-transform.ts` 等历史代码）；
  - 接入零外部依赖的轻量引擎，实现触控板双指无级缩放、双击微聚焦、滚轮平滑切图与下拉关闭；
  - 缩略图到大图的丝滑位移与渐进式锐化。

### 2. 文件夹封面与媒体墙现代简约排版
- **核心诉求**：原生优先、零风控、一眼辨识。
- **任务项**：
  - 100% 沿用 115 原生封面与图片缩略图 CDN，不引入任何可能触发风控的第三方网络请求；
  - 摒弃死板白方框，优化网格对齐、间距与圆角，做现代流媒体极简卡片；
  - 探索轻量虚拟渲染（Virtual Scrolling），解决几千项大目录下的滚动卡顿。

### 3. 设置面板开关中枢（风控兜底）
- **任务项**：在左侧栏底部现有设置面板中，设计增加「列表视频封面预览」与「媒体墙模式」总开关，方便大容量用户按需控权。

---

## 工程纪律与验收红线

1. **零污染原则**：任何针对 Vidstack 的预研与编码，严禁修改当前主分支的业务逻辑。
2. **不发明设计**：严格遵循 Vidstack 工业设计标准，拒绝在局部追加无序的 CSS 补丁。
3. **保留全部已有功能感知**：老播放器支持的文件移动、加收藏、选集预览、直链鉴权在新播放器中一个都不能丢。
4. **验证全绿方可收尾**：任何合并至主线的操作，必须保证 `typecheck`、`lint (0 error)` 与单元测试全部通过。
