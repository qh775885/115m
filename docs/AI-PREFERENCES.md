# AI-PREFERENCES

适用场景：

- 需要判断长期稳定偏好时
- 需求已明确确认，且后续容易遗忘或回退时
- 需要沉淀会长期影响实现判断的约定时

## 收录范围

这里只保留稳定、重复出现、长期有效的偏好或产品约定。

不收录：

- 一次性需求
- 临时修复过程
- 项目公共铁律
- 某个场景的操作步骤

## 当前稳定偏好

- 默认直接动手做，少把零碎步骤再丢给用户
- 发布 GitHub Release 时，默认写给用户看的更新说明，不写内部技术细节
- 文档尽量短：核心规则留在 `.trae/rules/project-baseline.md`，细节放到按需查看的 runbook
- AI 默认不要通读全部文档，应先看路由，再按任务加载必要文档
- 本地支线默认只做临时迭代，不主动推送；AI 负责判断何时可合并到 `main`
- 对用户已明确确认、且后续容易遗忘或回退的功能偏好、交互约定、实现边界，AI 应主动沉淀到这里
- 只要是值得长期保留、会影响后续实现判断的稳定偏好，AI 应主动沉淀到这里
- 图片查看器交互约定：正常模式下滚轮切图，缩放模式下滚轮缩放，双击负责进入或退出缩放模式
- 图片查看器滚轮切图原则：应优先保证结果可预期、顺序推进、避免一次手势直接跳多张；不同设备的原始 wheel 差异应由实现层吸收，而不是直接暴露给用户
- 测试建议原则：默认只要求用户测试本轮实际改动影响的功能点；只有确认存在严重耦合或高风险主链路时，才扩大手测范围
- 项目维护主原则：轻量化优先，以能稳定开发为目标；不要为了“完善”过度增加文档、规则、流程或智能体配置

## 维护规则

- 新偏好应优先合并或替换旧表述，不堆相似条目
- 如果某条偏好已经变成项目公共规则，应迁移到 `.trae/rules/project-baseline.md`
- 如果某条偏好只适用于单一流程或单一模块，应迁移到对应 runbook
