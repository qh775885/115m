# 迭代计划

> 记录后续迭代计划，按优先级与依赖顺序排列。计划可动态调整。
> **每项迭代内置对应测试覆盖**：做功能时顺手补它的回归测试，避免测试堆积到最后一次性大工程。
> 已完成的项会清理；实施前需重新评估影响面。

## 1. 测试覆盖补强（专项，按依赖顺序穿插）

> 历史盲区专项。按「依赖从小到大」推进，每补一个模块即锁定行为，为后续功能迭代打地基。

- [ ] **a. `lib/crypto.ts` + `lib/rsa.ts`（纯函数，最先做）**
  - m115 加解密、RSA 签名是下载/无损直链的安全核心，纯数学逻辑零依赖，测试成本最低、价值最高。
  - 覆盖：`m115_encode/decode` 往返、RSA 加解密、分块处理、边界输入。
- [ ] **b. `lib/pro-api.ts`（依赖 crypto）**
  - 下载 API 响应解析、签名参数组装、Web API 降级路径。
- [ ] **c. `content/core/preview.ts` 转码状态机**
  - 排队/完成/手动/失败/风控分支的状态判定（纯函数部分 `formatTranscodeStatus` 等）。
- [ ] **d. `content/core/unarchive-actions.ts` 解压流程**
  - 单文件/批量解压、密码跳过、分卷判断的纯逻辑。
- [ ] **e. `lib/videoThumbnail.ts`（依赖 clipper）**
  - 封面生成参数解析、缓存 key、兜底候选时间选择。
- [ ] **f. `platform/115/`（main-world、file-actions）**
  - 只能测少量纯逻辑（URL 构建、session 判定）；chrome.scripting 注入部分无法纯函数化，标记为「依赖注入、无法单测」。

## 2. 播放器体验增强（中）

> 每项完成后立即补对应回归测试。

- [ ] **a. 倍速记忆**：跨会话保存播放速度（`history.ts` 新增存储键）。
- [ ] **b. 播放列表续播**：记忆上次播到第 N 集（当前仅单集进度恢复）。
- [ ] **c. 字幕位置/字号/时间偏移调节**（当前字幕固定渲染，无拖拽）。
- [ ] **d. 快捷键可配置**（当前硬编码 `keyboard.ts`）。
- **测试**：倍速存储往返、续播位置存取、字幕设置持久化、快捷键配置解析。

## 3. 图片查看器增强（中，用户暂缓）

> 每项完成后立即补对应回归测试。

- [ ] **a. 下载/复制原图**（`originalUrl` 已就绪 `media-wall-images.ts:43`）。
- [ ] **b. 旋转**（CSS transform 已有 translate+scale，加 rotate 成本低）。
- [ ] **c. 图片信息面板**（尺寸/格式/路径）。
- [ ] **d. 重开记忆位置**（当前 `open()` 总是从 startIndex 重开 `:686`）。
- [ ] **e. 全屏 API**（requestFullscreen）。
- 附带修复：`toOriginalImageUrl` 正则 `/_\d+(\?|$)/`（`:10`）对多段尺寸 URL 会取错。
- **测试**：原图 URL 还原、旋转角度归一化、记忆位置存取。

## 4. 无损降级记忆（用户明确暂缓）

- **目标**：某视频从无损链路降级到 115 原画后，记录"该文件不适合无损"，下次打开直接走原画，跳过无损尝试。
- **现状**：`native-playback-monitor.ts` 已能检测卡死/黑屏/静音并自动降级，但每次打开都会重新尝试无损。
- **收益**：对高码率/编码不兼容文件减少重复卡顿等待。
- **风险**：需评估判断依据（pickCode 维度？）；文件可能被重新转码/源变化后"不适合"结论过期，需考虑失效机制。
- **测试**：降级记忆存取、失效机制判定。

## 5. 视频缩略图滚动抽帧限流（风控相关）

- **背景**：有用户反馈快速滚动大量视频可能触发风控。源头是快速滚动时多个视频同时进入封面抽帧，各自发 getM3u8 + 分片请求。
- **方向**：滚动抽帧限流（更激进的滚动防抖、或滚动速率高于阈值时暂停抽帧、或对未缓存视频先占位不抽帧）。
- **现状**：当前未实际遇到风控，暂不处理。若再次收到相关反馈则启动。
- **状态**：未排期。
