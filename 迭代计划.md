# 迭代计划

> 面向使用者：每项只写「现在的问题」和「解决后的好处」，不写技术细节。
> 技术细节由开发者在实施时自行对照代码，不在这里展开。

---

## 二、功能增强（中优先级，分批做）

### 2.1 播放器体验增强
- **a. 列表显示时长**：问题：列表不显示每个视频多长。好处：一眼看到时长。

> 其余（字幕调节/列表搜索/双击全屏/旋转快捷键）已按用户偏好暂缓，见 `.rules/开发记忆.md` 排除项 5。

### 2.2 图片查看器增强（暂缓）
- 问题：看图只能看，不能操作。好处：可下载/复制原图、旋转、查看尺寸格式信息、记住浏览位置、全屏。
- 附带修复：大图原图地址偶尔取错，修复后图片更清晰。

### 2.3 封面灯箱增强
- 问题：大图预览功能少。好处：支持全屏、一键打开 115 播放器、提前加载相邻图片更流畅。

### 2.4 无封面文件夹兜底显示
- 问题：没有封面的文件夹不显示，媒体墙出现空缺、不完整。
- 好处：无封面也正常显示，媒体墙完整不分裂。

### 2.5 侧边栏拖拽排序
- 问题：侧边栏顺序固定写死，只能开关。好处：可拖拽调整顺序并记住，重启不丢。

### 2.6 「115播放」按钮统一播放链路
- 问题：部分入口用旧方式打开播放器，上下文丢失。好处：所有入口统一走同一条链路，播放一致。

### 2.7 移动对话框体验
- 问题：移动文件默认进错目录、搜索偶有错乱、不能用键盘操作。
- 好处：默认进当前文件所在目录、搜索更稳定、支持键盘导航。

### 2.8 播完面板取消连播
- 问题：播完自动连播，3 秒倒计时无法取消。好处：播完可点「取消连播」。

### 2.9 顶栏长标题悬浮提示
- 问题：标题太长被省略，看不到全名。好处：鼠标悬停显示完整标题。

---

## 三、工程级改进（低优先级，穿插做）

> 内部质量改进，使用者无感知，但让项目更稳、更好维护。

- **3.1 权限精简**：去掉未实际使用的网站访问权限，更安全。
- **3.2 清理无用代码**：移除废弃的本地播放历史相关代码，减小体积。
- **3.3 缓存整理**：统一缓存存储格式，启动时自动清理旧数据。
- **3.4 错误统计**：加轻量统计埋点，便于发现和定位失败链路。
- **3.5 自动检查**：接入 GitHub Actions 自动跑检查，防止改坏功能。
- **3.6 播放源逻辑合并**：两处重复的 m3u8 逻辑合一，行为一致。
- **3.7 多窗口操作收敛**：多账号多窗口时，删除/移动只刷新对应窗口，不互相干扰。
- **3.8 说明文档补全**：README 更新过时内容。
- **3.9 临时文件清理**：删除遗留调试文件。

---

## 四、测试覆盖补强（专项，穿插做）

> 给关键模块补自动化测试，锁定现有行为，为后续改动打地基。面向开发者，不展开细节。

- [ ] **a. 加解密核心**（最先做，其他依赖它）
- [ ] **b. 下载接口解析**（依赖 a）
- [ ] **c. 转码状态机**
- [ ] **d. 解压流程**
- [ ] **e. 视频封面处理**（依赖 clipper）
- [ ] **f. 115 平台纯逻辑**

---

## 五、暂缓 / 未排期

- **5.1 无损降级记忆**（暂缓）：记住「该文件不适合无损」，下次直接播原画。
- **5.2 缩略图滚动抽帧限流**（未遇到）：快速滚动抽帧可能触发风控，收到反馈再启动。
- **5.3 播放列表拖拽排序**（暂缓）：收益存疑。
- **5.4 下载拦截体验闭环**（暂缓，明确先不做）：后续发现问题再捡起。
