# 迭代计划

> 记录后续迭代计划，按优先级与依赖顺序排列。计划可动态调整。
> **每项迭代内置对应测试覆盖**：做功能时顺手补回归测试，避免测试堆积到最后。
> **大工程穿插做**：测试专项、工程改进等不阻塞功能迭代，按依赖顺序穿插推进。
> 已完成的项会清理；实施前需重新评估影响面。

---

## 一、高优先 bug / 风险修复（先做）

> 用户可感知的异常行为或安全边界，纯 bug 成本低、价值高。

### 1.7 OPEN_TAB 无 URL 白名单（中高，安全）
- `background/index.ts:125-133` 受信 sender 可让扩展打开任意 URL，与其他消息（MAIN_WORLD_FETCH/DOWNLOAD/SET_COOKIE）白名单不一致。
- 方向：加 115 域白名单校验。
- 测试：白名单判定函数。

### 1.8 并发转码共享 115vod tab 被误关（中）
- `platform/115/main-world.ts:99-118` 单 id 记录 + `transcode.ts` finally 关闭 → 并发转码时 A 关掉 B 在用的 tab。
- 方向：引用计数或 session 级管理。
- 测试：tab 复用/释放计数逻辑。

### 1.9 人机验证标签页无限叠加（中）
- `lib/drive115.ts:92-101` 911 每次失败都开新 tab，无去重。
- 方向：单 tab + 幂等去重。
- 测试：去重逻辑。

---

## 二、功能增强（中，分批做，每项补测试）

### 2.1 播放器体验增强
- **a. 播放列表续播**：记忆上次播到第 N 集。
- **b. 字幕位置/字号/时间偏移调节**（当前固定渲染无拖拽）。
- **c. 播放列表搜索/过滤**（`overlay.ts:659-697` 侧栏加关键词过滤）。
- **d. 播放列表项显示时长**（`overlay-playlist.ts` duration 已取到，未展示）。
- **e. 双击视频全屏**（`events.ts:164-168` 双击当前无动作）。
- **f. 旋转快捷键 R**（`rotation-manager.ts` 现有控件循环 90°）。
- 测试：续播位置、字幕偏好持久化、列表过滤、时长渲染。

### 2.2 图片查看器增强（用户暂缓）
- **a. 下载/复制原图**、**b. 旋转**、**c. 信息面板（尺寸/格式/路径）**、**d. 重开记忆位置**、**e. 全屏**。
- 附带修复：`toOriginalImageUrl` 正则 `/_\d+(\?|$)/`（`media-wall-images.ts:10`）多段尺寸取错。
- 测试：原图 URL 还原、旋转归一化、记忆位置存取。

### 2.3 封面灯箱增强（中）
- `preview.ts:184-244`：加全屏、「打开 115 播放器」按钮（pickCode 已知）、相邻帧预加载。
- 测试：灯箱按钮触发、预加载逻辑。

### 2.4 无封面文件夹兜底显示（中）
- `media-wall-folders.ts:190-191` 无 `img_url` 的文件夹被丢弃、媒体墙分裂。用默认文件夹图标兜底。
- 测试：无封面时仍生成卡片。

### 2.5 侧边栏拖拽排序（中）
- `sidebar.ts:26-38` specialOrder 写死；设置面板只能开关。加拖拽排序 + 顺序持久化。
- 测试：顺序读写、拖拽后存储。

### 2.6 「115播放」按钮统一播放链路（中）
- `action-buttons.ts:25-26` 走 `window.open` 丢上下文；改统一走 `openPlayer()`（OPEN_TAB，带 fileId/cid/playlistToken）。
- 测试：按钮点击调 openPlayer、参数组装。

### 2.7 移动对话框体验（中）
- `move-dialog.ts:319` 默认进「最近移动」目录而非当前文件目录；`:360` 搜索竞态无序号；`:542` 无键盘导航；`move-dialog-api.ts` 列表/搜索上限。
- 测试：目录定位、搜索防抖竞态、键盘导航。

### 2.8 播完面板取消连播倒计时（中）
- `player-navigation.ts:20` 3 秒硬编码；`overlay.ts:285-296` 无取消按钮。加「取消连播」。
- 测试：倒计时计划/取消分支。

### 2.9 顶栏长标题悬浮提示（低）
- `overlay-header.ts:84-86` 省略号无 title 提示。加 `title` 属性或悬停滚动。
- 测试：标题节点含 title。

---

## 三、工程级改进（低，穿插做）

### 3.1 manifest 权限收敛
- `wxt.config.ts:35-45` 的 `aps.115.com`、`cdnfhnfile.115cdn.net` 源码零引用；`constants.ts:8-10` 移除对应常量。
- 测试：无（typecheck/build 验证）。

### 3.2 本地播放历史死代码收敛
- `history.ts:79` LOCAL_PLAY_HISTORY_ENABLED=false，但 `background/history-store.ts` 与 `GET/SET/DELETE_HISTORY` 消息仍注册、`player.ts:1389` 还发 DELETE_HISTORY。二选一：彻底移除或作为 native 历史离线 fallback。
- 测试：移除后消息类型清理；或启用后往返。

### 3.3 数据缓存版本迁移与清理
- `videoThumbnail.ts:19` CACHE_VERSION='v4' 手动递增，旧 key 永不清理；播放器偏好裸 key 无 schema。
- 方向：统一 storage schema 版本 + 启动迁移/清扫。
- 测试：迁移函数、旧 key 清理。

### 3.4 错误监控轻量埋点
- 全项目仅 console；`runtime.ts` 重试耗尽处加轻量计数点，便于量化失败链路。
- 测试：埋点触发条件。

### 3.5 CI 接入
- 无 `.github/`；`release-check.mjs` 检查的 workflow 不存在。补 GitHub Actions：push 跑 `pnpm check`。
- 测试：无（脚本可跑通）。

### 3.6 handleFetchM3u8 双实现收敛
- `background/media-info.ts` 与 `lib/drive115.ts` 同一 m3u8 源两套逻辑（911 人机验证/errNo 处理不一致）。收敛为一个。
- 测试：解析分支（含 911）。

### 3.7 多标签/多账号文件操作广播收敛
- `file-operations.ts:67` query115Tabs 查所有 115 tab，删除/移动刷新会波及其他账号窗口；双通道冗余（sendMessage + executeScript 重复删）。
- 方向：按 sender 窗口/账号收敛，去重保留单通道。
- 测试：tab 过滤函数。

### 3.8 README 补全
- 补转码 VIP 加速、图片查看器增强、权限说明等过时项。

### 3.9 临时文件清理
- `temp/transcode_debug/` 残留，按 AGENTS.md 纪律删除。

---

## 四、测试覆盖补强（专项，穿插做，按依赖顺序）

> 历史盲区专项，按「依赖从小到大」推进；每补一块即锁定行为，为后续功能迭代打地基。

- [ ] **a. `lib/crypto.ts` + `lib/rsa.ts`（纯函数，最先做）**：m115 加解密往返、RSA 加解密、分块、边界输入。
- [ ] **b. `lib/pro-api.ts`（依赖 crypto）**：下载响应解析、签名组装、Web API 降级。
- [ ] **c. `content/core/preview.ts` 转码状态机**：`formatTranscodeStatus` 排队/完成/手动/失败/风控分支。
- [ ] **d. `content/core/unarchive-actions.ts` 解压流程**：单文件/批量/密码跳过/分卷纯逻辑。
- [ ] **e. `lib/videoThumbnail.ts`（依赖 clipper）**：封面参数解析、缓存 key、兜底候选时间。
- [ ] **f. `platform/115/`**：仅纯逻辑（URL 构建、session 判定）；chrome.scripting 注入部分标记「无法纯函数化」。

---

## 五、暂缓 / 未排期

### 5.1 无损降级记忆（用户明确暂缓）
- 记录"该文件不适合无损"，下次直接走原画。风险：判断依据、失效机制。
- 测试：降级记忆存取、失效判定。

### 5.2 视频缩略图滚动抽帧限流（风控相关）
- 快速滚动多视频并发抽帧可能触发风控。未实际遇到，收到反馈再启动。

### 5.3 播放列表拖拽排序（低，收益存疑）
- 服务端目录顺序一般由文件名排序决定，临时列表拖拽意义有限。

### 5.4 下载拦截体验闭环（暂缓，用户明确先不做；无反馈不处理，后续发现问题再捡起）
- `download-intercept.ts:14-40`：无幂等守卫、双击重复、未注入鉴权 cookie、成功静默/失败 alert。
- 方向：按钮二次点击禁用+去抖；解析 `auth_cookie` 先注入；成功 toast + 「复制直链」入口（给 IDM/Aria2）。
- 测试：解析逻辑、去抖、cookie 参数组装。
