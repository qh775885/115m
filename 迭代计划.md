# 迭代计划

> 目标：把项目做成**架构优质、干净整洁、健康可持续、稳定高性能**。核心功能：**播放器 + 看图器**。
> 发展驱动：深度审计 + 用户反馈双轨（见 `.rules/迭代否决项.md` 第四节）。
> 测试：改到哪补到哪，杂项与测试穿插在每步中做。

---

## 📊 当前进度（2026-08-14）

- P0-P5 已完成（v1.8.9 发布 → 架构筑基 → P5 深化，426 基线）；v1.9.0 用户实测中，无新增问题反馈。
- **P6 全局架构审计已完成**：清死代码 → 重复收敛 → content 大文件拆分 → player 修复 → lib/shared 收敛 → 测试补强；期间修复「无损画质丢失」（MAIN world 注入闭包序列化失效）。
- **审计结论**：架构进入冲刺期，无新增真实功能缺陷（P5 的 destroy 泄漏/预载并发已修复未复发）。剩余债 = 重复（收口近尾声）+ 大文件（preview/lightbox/videoThumbnail/player）+ 死代码/测试盲区（已清一批，background 等仍待补测）。
- **降级项**：videoThumbnail 拆分、createArtplayer 拆分、crypto 往返测试（RSA 测试环境 >60s/次，性能瓶颈非逻辑缺陷）。
- **v1.9.1 起：看图器专项迭代启动**（见下章节）。当前累计：测试 **484 个**；typecheck / lint(0 error) / build 全绿。

---

## 🖼 看图器专项迭代（v1.9.1 起）

> **定位**：看图器性能、交互、手感、UI、图标做到最优。
> **用户核心手感**：滚轮切图为主；点击放大后滚轮接管缩放，还原后滚轮接管切图。
> **现有基础**：缩放/拖动+惯性回弹/滚轮切图/缩略图栏/删除/键盘/邻居预载/选中同步/右键委托已齐全；`viewer-transform.ts` 纯函数 14 测试；`NeighborPreloader` 已可测。
> **不做的**：拖拽切图手势（用户明确不用）；Fancybox 等外部库（已定论自研）。

### 已知短板（本轮要解决）

| # | 问题 | 现状依据 |
|---|---|---|
| 1 | 切图无过渡动画 | `render()` 直接换 `imageEl.src` + `resetZoom()`，无过渡，视觉硬切 |
| 2 | 预载未命中白屏 | 预载并发 4，快速滚动跟不上时直接白屏等待，无 loading 兜底 |
| 3 | 滚轮增量未归一化 | `WHEEL_GESTURE_STEP=70` 硬编码，未按 `deltaMode` 换算，部分环境滚很多下才切 |
| 4 | 节流锁 120ms 吞滚动 | `wheelGestureTriggered` 锁 120ms，快速连滚事件间隔常 <120ms，切图滞后 |
| 5 | 大文件难维护 | `media-wall-images.ts` 799 行，`createLightboxController` 单闭包 ~610 行 |
| 6 | UI/图标打磨空间 | 样式集中在 `media-wall-images.css`，图标用 `Icons` 内联，未统一打磨 |

### 迭代步骤（每步 typecheck/lint/test/build 全绿再下一步）

| 步骤 | 内容 | 涉及文件 |
|---|---|---|
| G1 滚轮手感 | ✅ 完成：抽 `viewer-wheel.ts` 纯函数（`deltaMode` 行/页/像素归一化 + 累积切图决策），消除 120ms 冷却锁滞后，切后立即响应下一滚；+11 测试 | `viewer-wheel.ts`（新）+ `media-wall-images.ts` |
| G2 切图过渡 | ✅ 完成：双图 crossfade（旧图保持到新图就绪，就绪后淡入过渡落地）；预载未命中显示 loading 占位（120ms 延迟防闪）；版本号 guard 防快速连滚竞态；+2 测试 | `media-wall-images.ts` + `media-wall-images.css` |
| G3 缩放体验 | 点击放大/还原、滚轮缩放接管逻辑打磨（缩放阻尼、光标锚点、边界回弹）；放大后拖动平移惯性优化 | `media-wall-images.ts` + `viewer-transform.ts` |
| G4 预载策略 | `NeighborPreloader` 参数调优（错峰/并发/偏移序列按需）；预载命中率可观察 | `media-wall-preload.ts` |
| G5 UI 打磨 | 看图器样式统一：工具栏/缩略图栏/按钮/过渡动效规范；浅色模式下一致性 | `media-wall-images.css` |
| G6 图标统一 | 看图器图标走 `Icons` 统一源（放大/缩小/关闭/缩略图等），杜绝散落内联 | `media-wall-images.ts` + `shared/icons.ts` |
| G7 拆分+测试 | `createLightboxController` 拆分（手势/节流/切换调度抽纯函数）；补 G1/G2 逻辑测试；保 471 基线不降 | `media-wall-images.ts` + 新 `.test.ts` |

### 验收标准

- 滚轮切图：快速连滚不滞后、不误触发，每滚一步即切一张，有平滑过渡、无白屏。
- 滚轮缩放：放大后滚轮立即接管缩放，还原后立即交还切图，切换无迟滞。
- 架构：手势/节流/切换调度可测，测试数不降反升。

---

## 未做清单（低优先/暂缓，遇新证据再评估）

| 项 | 说明 |
|---|---|
| L7 | unarchive 目录 limit 业务边界 |
| L8/L12 | 看图器/侧边栏 doc 级 keydown 常驻监听（observer 统一时评估） |
| L10 | 后台缓存 Map 惰性清理（版本清扫已做，age 淘汰待评估） |
| L15 | messages 反向 import（已定论：合理，不改） |
| L16 | 双 utils 命名（core 侧改 dom-utils） |
| L20 | main-world 命名（暂缓） |
| lint any | 既有宽松风格，0 错误，不强制清零 |
| 3.4 看图器交互调整 | 用户暂缓，等反馈 |
| content 6 处 L 级资源残留 | sidebar observer / refreshTimers / frame load / preload timer / lightbox overlay 常驻 / preview iframe 清理死角（无实害，页面级常驻可接受） |
| player 5 处 L 级 timer 残留 | hover-preview once / subtitle setTimeout / move-dialog searchTimer / button click 监听 / player-volume 监听（均有 destroy guard） |
| telemetry 失败计数只增不清 | 上限低，暂缓 |
| crypto/rsa 往返测试 | 512-bit RSA 模幂测试环境 >60s/次，性能瓶颈非逻辑缺陷，暂缓 |
| 控件构建结构性重复 | 5 个 artplayer control 工厂化（收益一般，暂缓） |

---

## 收尾约定

- 未做项不是永久关闭：深度审计或用户反馈提供新证据时重新评估。
- 每步完成后：typecheck / lint / 相关测试 / build 全绿再进入下一步。
- **当前基调（2026-08-14 定）**：
  - 项目定位 = **维持健康稳定**，不做主动重构、不扩功能，等待用户实测反馈。
  - 架构审计改为**按需触发**：有具体痛点、用户反馈或引入大改动时才做针对性审计，不再固定轮次。
  - 日常健康度看门狗：**471 测试全绿 + lint 0 error + build 通过** 即视为健康，达标不多动。
  - **1.9.0 发布由用户决定并标记**；版号停留期间以基础稳定为唯一目标。
