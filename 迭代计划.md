# 迭代计划

> 目标：1.9.0 前做到又快又稳、低开销高性能，功能增强暂停，遇到实际问题再说。
> 测试：改到哪补到哪，顺手补关键测试。

## 2026-08-12 全量性能/稳定性审计

范围：background / content / player / lib 全模块。行号已逐一核对源码。
状态：✅ 已修复　⚠️ 需确认行为后做　⏳ 待处理

### P0 稳定 — 弱网或长播放可能直接坏掉

1. ✅ **播放进度"周期写"永远不触发**：`timeupdate` 高频调用每次重置 3s debounce，定时器永不到期，连续看完关页签丢整段进度。已改为节流型写入（首次/满区间 leading 立即写 + 不重置的 trailing 定时器兜底），补充连续播放周期落库测试。
2. ✅ **稳态 HLS fatal 错误无人处理**：原守卫 `if (!switchUrlInFlight || !fatal) return` 导致稳定播放中 fatal 错误被忽略、画面永久冻结。已加稳态自愈 `handleSteadyHlsFatalError`：network 错误限量 `startLoad(-1)`（上限 3，播放成功清零），media 错误 `recoverMediaError()` 一次，额度用尽 toast 提示不再无提示卡死。⚠️ HLS 卡顿看门狗（timeupdate 驱动的 stall 检测）涉及降级策略，待确认后再做。
3. ✅ **弱网挂起链**：超时原先只覆盖到响应头。已扩展——`promise.ts` 的 `fetchWithTimeout` 现覆盖「响应头+body 读取」两阶段（text/arrayBuffer/json 读取挂起也会被 abort 打断）；`drive115.ts` 全部 fetch 挂 15s 超时；115vod 串行队列每一环挂 20s 超时，队列单步再套 45s 兜底竞速，页面注入 fetch 用 `AbortSignal.timeout` 特性探测后挂载。补充 body 阶段超时测试。
4. ✅ **DecoderFlow 读块重入竞态**：`autoReadChunk` 加在途 Promise 互斥（`readChunkInFlight`），`ondequeue` 改走 `triggerAutoReadChunk()` 并挂 catch，消除同区间重复 Range、数据乱序与 unhandled rejection。

### P0 正确 — 会点开错误的视频

5. ✅ **li 复用后播放绑定不刷新**：`home-play-binder` WeakSet 改为「元素↔pickCode」WeakMap，pickCode 不一致或不再是视频时先解绑旧监听再重绑。补充 li 复用/复用为非视频两条回归测试。
6. ✅ **download-intercept 无幂等**：对 downloadNode 加「元素↔pickCode」WeakMap 幂等标记，pickCode 变更才解绑旧监听重绑，消除监听叠加导致的多次解析/多次开窗/旧 pickCode 下载。

### P1 性能 — 启动与播放热路径

7. ✅ **SW 每次唤醒全量解析 storage**：`cache-sweeper` 先只读版本 key 比对，版本一致直接返回，避免每次唤醒全量反序列化封面 base64；仅版本变化（升级后一次性）才 `get(null)`。
8. ✅ **切无损/原生源不销毁旧 hls 实例**：新增 `disposeHlsInstance()`，`switchQuality` 切到 native、`switchToVideo` 目标为 native 时主动销毁旧 hls（含 blob URL 回收与代次失效），防止 worker/定时器泄漏与 recover 误写 src。
9. ⏳ **悬停精确队列无上限、无取消**：复查后**无需大改**——队列已有去重（`preciseQueue.includes` + `preciseCovers.has`）、`hoverRequestVersion` 版本失效保护、destroy 清空队列；真实影响仅"快速划过时在途请求无法中断"。改动风险大于收益，保持现状。
10. ⚠️ **每集自动预热最多 180 张粗封面 + 1.2s 后精化一批**：复查后实际开销低于审计预期——180 张仅在 >180min 超长视频触发（普通视频 24~72 张），且有 timeline/storage 缓存 + 签名去重。收敛数量涉悬停出图体验，属行为变化，待确认后做。
11. ✅ **清单重复拉取**：`getVideoCoverAt` 失败结果被永久缓存为 null 不再重试 → 已改为失败（null）不缓存、下次重试；播放器持有 m3u8List 后经 `primeThumbnailSourceUrl` 预置缩略图源，悬停首次不再重复 `getM3u8`。
12. ✅ **每次 initHls 重复拉 master**：`fetchMasterPlaylistText` 加 30s 内存缓存（按 pickCode 失效），`hydrateFromMasterPlaylist` 复用同一文本（不再直接 fetch 同 URL），每集/切画质仅拉一次 master。
13. ⏳ **home DOM 热路径堆叠**：复查后**暂不重构**——已有 rAF 合并、media-wall 的 `sameItemsRef`/signature 短路、scannedPickCodes 去重三层防护；审计列出的"6+ observer"真实存在，但逐一合并风险高、收益不确定，留待出现实际卡顿反馈再处理。

### P2 内存与杂项

14. ⏳ `player-settings-menu.ts:186` 设置控件 MutationObserver 从不 disconnect、每切一集新增并持悬空 DOM；`player-selector.ts` 模块 Set 持旧控件强引用。
15. ⏳ 封面内存缓存按条数而非字节设限；时间轴缓存按 pickCode 单调增长、每次全量 get+set 趋 O(N²)。
16. ⏳ 音轨重建两个 `once` 失败残留误 seek；native-monitor 双降级并发竞态、暂停时无限轮询；字幕 rAF 每帧无变化也写 DOM。
17. ⏳ 转码状态轮询每次串行 3~5 请求无去重；`FETCH_PLAYLIST` 每次全量 1150 条 MB 级双重序列化；孤儿隐藏 115vod 标签累积。
18. ⏳ wangpan iframe 换文档后旧 Document 注册表不清理；scannedPickCodes 10000 全清引发重注入风暴；扫描管线无模块化 try/catch。
19. ⏳ 其余低危：transcode cooldown Map 无上限、`waitForTabComplete` 竞态窗口、telemetry 写失败丢批、logger 热路径先拼串、NALU 重组 2 倍拷贝、`BoundedCache.get` 不刷新 LRU、HlsIO destroy 不 abort、灯箱预载 ±2 不可取消、滚动中高频全量读写 sessionStorage、音量拖动逐次同步写 localStorage、overlay 播放列表无分片渲染。

### 进度小结（2026-08-12）

- 已完成：P0-1~6、P1-7~8、P1-11、P1-12（10 项），typecheck / lint / 241 测试 / pnpm build 全绿。
- 复查无需改动：P1-9（悬停队列已有去重+版本失效保护）、P1-13（home 已有三层短路，重构风险大于收益）。
- 待确认：P0-2 HLS 卡顿看门狗降级策略、P1-10 预热数量收敛（均涉行为变化）。
- 待处理：P2-14~19（按优先级后续迭代）。
