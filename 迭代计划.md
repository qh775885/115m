# 迭代计划

> 定位：**轻量维护版**。放弃「按计划推进功能优化」路线，改为**实测反馈驱动**——只在使用中发现问题再针对性修复/调优，不做无依据的主动功能调整。
> 底线：**底层稳定 + 架构完美是方向，不是凑活**。任何修复都不得以「先能跑」的方式实现，必须保持架构质量。

---

## 当前状态（2026-08-14）

- P0-P6 架构审计完成，无严重耦合模块；循环依赖已消除，死代码已清。
- 健康基线：测试 **484 个**全绿；typecheck / lint(0 error) / build 全绿。

---

## 工作模式

1. **反馈驱动**：使用中发现真实问题 → 记录 → 分级 → 针对性修复。用户主动反馈的优先级最高。
2. **修复质量要求**：任何修复必须保持架构质量——可测、不引入新耦合、不留死代码/临时凑活。改到哪补测到哪。
3. **不做的事**：无真实痛点时，不做主动重构、不扩功能、不按旧计划推进 G 系列。
4. **架构审计按需触发**：有具体痛点、用户反馈或引入大改动时才做针对性审计，不固定轮次。

---

## 健康度看门狗

- 484 测试全绿 + lint 0 error + build 通过 = 健康，达标不多动。
- 若修复导致测试下降，必须补回，不接受"懒得补"。

---

## 暂缓清单（低优先/暂缓，遇新证据再评估）

| 项 | 说明 |
|---|---|
| L7 | unarchive 目录 limit 业务边界 |
| L8/L12 | 看图器/侧边栏 doc 级 keydown 常驻监听（observer 统一时评估） |
| L10 | 后台缓存 Map 惰性清理（版本清扫已做，age 淘汰待评估） |
| L15 | messages 反向 import（已定论：合理，不改） |
| L16 | 双 utils 命名（core 侧改 dom-utils） |
| L20 | main-world 命名（暂缓） |
| lint any | 既有宽松风格，0 错误，不强制清零 |
| content 6 处 L 级资源残留 | sidebar observer / refreshTimers / frame load / preload timer / lightbox overlay 常驻 / preview iframe 清理死角（无实害，页面级常驻可接受） |
| player 5 处 L 级 timer 残留 | hover-preview once / subtitle setTimeout / move-dialog searchTimer / button click 监听 / player-volume 监听（均有 destroy guard） |
| telemetry 失败计数只增不清 | 上限低，暂缓 |
| crypto/rsa 往返测试 | 512-bit RSA 模幂测试环境 >60s/次，性能瓶颈非逻辑缺陷，暂缓 |
| 控件构建结构性重复 | 5 个 artplayer control 工厂化（收益一般，暂缓） |

---

## 收尾约定

- 暂缓项不是永久关闭：深度审计或用户反馈提供新证据时重新评估。
- 每次修复完成后：typecheck / lint / 相关测试 / build 全绿才算完。
- **项目基调（2026-08-14 定）**：
  - 定位 = 底层稳定、架构健康的实测调优期；不按计划扩功能，只修真实问题。
  - 修复必须符合架构方向，不凑活、不留技术债。
  - 版号停留期间以基础稳定为唯一目标。
