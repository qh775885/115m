# 迭代计划

> 目标：把项目做成**架构优质、干净整洁、健康可持续、稳定高性能**。核心功能：**播放器 + 看图器**。
> 发展驱动：深度审计 + 用户反馈双轨（见 `.rules/迭代否决项.md` 第四节）。
> 测试：改到哪补到哪，杂项与测试穿插在每步中做。

---

## 📊 当前进度总览（2026-08-14 P6 完成）

| 阶段 | 状态 | 说明 |
|---|---|---|
| P0-P5 已完成（v1.8.9 发布 → 架构筑基 → P5 深化） | ✅ | 426 测试基线，见下方历史 |
| v1.9.0 用户实测 | 🔄 | 无新增问题反馈 |
| **P6 全局架构审计** | ✅ | 六步完成，见下方执行记录 |

**当前累计**：测试 **471 个**（P5 基线 426，+45）；typecheck / lint(0 error) / build 全绿。

---

## P6 全局架构审计结论（2026-08-14，第六轮）

### 判断：架构已进入「重复收敛 + 大文件拆分 + 测试补强」冲刺期，**无新增真实功能缺陷**

三域并行深度审计（content / player / lib+platform+shared+background）完成。与 P5 相比最大的变化：**P5 确认的 2 个真实缺陷（destroy 泄漏 / 预载并发）已修复且未再出现同类问题**；本轮未发现新的 H 级正确性缺陷。剩余问题全部集中在三类架构债：

1. **复制粘贴式重复仍在（最多的问题类）**：
   - `getItemTitle`/`getItemName` 函数体完全相同（`native-dom.ts:29` / `:118`）。
   - folders/images 选择按钮构建块（~20 行）逐行复制（`media-wall-folders.ts:135-154` / `media-wall-images.ts:751-769`），`syncSelectionState` 近乎相同。
   - 音量偏好**双路径重复写入** localStorage：`player.ts:665-671`（onVolumeChange）与 `player-volume.ts:123-158`（volumechange/slider/btn 三处）。
   - `customType.m3u8` 回调（`player.ts:456-501`）与 `PlayerQualityController.switchQuality/ensureOriginalSourceLoaded` 逻辑重复。
   - 时间格式化双实现（`formatPlaylistSeconds` vs `formatTimeLabel`）、名称提取/cid 解析/MouseEventInit 各 3-4 个变体。
   - `parseJsonText`/`parseJsonOrNull`、上下文失效检测双实现（`videoThumbnail.ts:101` vs `runtime-utils.ts:9`）。

2. **单体大文件收敛（P5 遗留核心，玩家侧已拆完，内容侧仍在）**：
   - `preview.ts`（875 行）6 职责混居：M3U8 取流 / 转码文案 / 封面 lightbox（跨文档全局单例）/ observer 注册表 / 预览渲染管线 / 转码按钮状态机。
   - `media-wall-images.ts`（817 行）`createLightboxController` 单闭包 **618 行**（22 内部函数 / 26 状态变量），DOM/物理/导航/删除/事件全挤一起，缩放物理 0 测试。
   - `videoThumbnail.ts`（617 行）厚门面：源 URL 解析 / 封面生成 3 策略 / 缓存层 8 职责混居。
   - `player.ts`（1030 行）`createArtplayer` 单方法 396 行，组合根 attach 300 行。

3. **死代码/无效链路（P5 收口后又有新发现）**：
   - `enableTranscodeFrameFallback = false`（`preview.ts:716`）整条 iframe 回退链路 ~50 行死代码 + 1 条消息协议永不执行。
   - `renderFoldersSection` 未用的 `forwardNativeContextMenu` 参数死管道（`media-wall-folders.ts:72` / `media-wall.ts:60-62`）。
   - `Drive115.setDownloadCookie`（`drive115.ts:197-210`）全仓零引用。
   - `formatVttTime`（`hover-utils.ts:17`）仅测试引用。

4. **测试盲区（H 级 4 处）**：
   - `background/index.ts` 安全路由（isTrustedSender / URL 白名单 / cookie 域名校验）。
   - `background/transcode.ts` 核心状态机（transcodeOne / handleTranscodeStatus / batch）。
   - `lib/crypto.ts` + `rsa.ts` + `pro-api.ts` 加密下载链路往返。
   - `platform/115/main-world.ts` 四模式回退 + 串行队列。

### 分层健康度评估表（对比 P5）

| 维度 | P5 结论 | P6 结论 |
|---|---|---|
| 依赖方向 | ✅ 无环 DAG | ✅ 无环 DAG（lib↔shared 交叉依赖仍存在，M 级待收敛） |
| 消息契约 | ✅ 收敛 | ✅ 18 种一一对应；`TRANSCODE_FRAME_READY` 因死代码成废协议 |
| 资源管理 | ⚠️ destroy 补齐 | ✅ 无 H 级泄漏；L 级残留：sidebar observer、refreshTimers、frame load、preload timer |
| 单体 | ⚠️ 玩家侧已好 | ✅ 玩家侧全清；content 侧 preview/images 待拆 + player.ts createArtplayer |
| 重复 | 🔴 最多问题类 | 🔴 仍有 H 级重复（native-dom、选择同步、音量双写、m3u8 回调） |
| 可测性 | ⚠️ 纯函数方向正确 | ⚠️ content 物理/状态机盲区 + background 安全/转码盲区 |

### 核心策略

1. **先清死代码**：低风险高确定性，腾出真实结构。
2. **重复收口**：H 级重复每处都有明确收敛目标（native-dom 合并、选择同步抽公共、音量偏好单一入口、m3u8 回调委托 quality 控制器）。
3. **content 大文件拆分**：preview.ts 三拆（transcode-button / cover-lightbox / observer-registry），images lightbox 抽物理纯函数。
4. **补测试盲区**：background 安全路由 + 转码状态机 + crypto/rsa/pro-api 是最高价值。
5. **穿插执行**：每步带测试 + 顺手清理 L 级杂项。

---

## P6 执行步骤

### 步骤 0：死代码与无效链路清理 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 0.1 | 删除 `enableTranscodeFrameFallback` 整条 iframe 链路（preview.ts 死代码 + messages.ts / background / main-world.ts / transcode.ts 中 `TRANSCODE_FRAME_READY` 协议整链删除） | ✅ |
| 0.2 | 清理 `renderFoldersSection` 死参数 `forwardNativeContextMenu` 及 `media-wall.ts` 无效管道，删除 `native-interact.ts` 死函数 | ✅ |
| 0.3 | 删除 `Drive115.setDownloadCookie`（全仓零引用） | ✅ |
| 0.4 | 删除 `formatVttTime`（仅测试引用） | ✅ |
| 0.5 | debug 挂载失效修复（`window.playerManager` 移至 initPlayer 后挂载） | ✅ |

> 步骤 0 全量验证：typecheck / lint(0 error) / test(425) / build 全绿。

### 步骤 1：重复收敛（content 域优先） ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 1.1 | `native-dom.ts` `getItemTitle`/`getItemName` 合并为单函数（extractors/unarchive 调用方已切换） | ✅ |
| 1.2 | folders/images 选择按钮块抽 `createWallSelectionButton`，`syncSelectionState` 抽 `syncWallSelectionState` 公共工具 | ✅ |
| 1.3 | 选择同步魔法数收敛为命名常量（`SELECT_SYNC_AFTER_ACTION`/`SELECT_SYNC_INITIAL`/`SELECT_SYNC_AFTER_DRAG_SELECT`） | ✅ |
| 1.4 | cid 解析统一 `getItemParentId`（native-dom），MouseEventInit 收敛 `buildMouseInit` | ✅ |
| 1.5 | `WEB_API_URL`/`WALL_HIDDEN_CLASS` 常量使用收敛（9 处硬编码 URL / 3 处隐藏 class 字符串） | ✅ |

> 步骤 1 全量验证：typecheck / test(425) 全绿。

### 步骤 2：content 大文件拆分 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 2.1 | `preview.ts` 拆 `transcode-button.ts`（转码状态机 ~300 行独立） | ✅ |
| 2.2 | `preview.ts` 封面 lightbox 独立 `cover-lightbox.ts`（消除跨文档全局单例隐患） | ✅ |
| 2.3 | `preview.ts` observer 注册表独立 `observer-registry.ts`（home.ts 同步切换导入） | ✅ |
| 2.4 | `createLightboxController` 抽缩放/拖拽物理纯函数 `viewer-transform.ts`（14 个测试） | ✅ |

> 步骤 2 成果：preview.ts 875→~360 行（6 职责 → 4 模块）。全量验证：typecheck / lint(0 error) / test(**439**) / build 待步骤 6。

### 步骤 3：player 域重复与封装修复 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 3.1 | `customType.m3u8` 回调 + restart 事件委托 `PlayerQualityController.resolveQualityPlayback`（消除原画占位三连重复） | ✅ |
| 3.2 | 音量偏好单一写入入口（移除 player.ts onVolumeChange 双写 + player-volume.ts 冗余主动保存） | ✅ |
| 3.3 | 修复 `player-media-track.ts` 私有属性越权（SubtitleController 增加 getSubtitleItems/getSelectedSubtitleSid） | ✅ |
| 3.4 | `readOverlayMetaFromQuery` 下沉 `player-query.ts`，切断 player-actions → overlay 大模块依赖 | ✅ |
| 3.5 | `HoverCover` 双份定义合并（hover-utils 权威，session re-export） | ✅ |
| 3.6 | seek 重置收敛 `resetVideoProgress`（media.ts） | ✅ |
| 3.7 | `formatPlaylistSeconds`/`formatTimeLabel` 收敛为 `formatTimeLabel`/`formatCompactTime` | ✅ |
| 3.8 | 音轨标签生成收敛 `buildAudioTrackLabel`（audio-manager） | ✅ |

> 步骤 3 全量验证：typecheck / lint(0 error, 145 warn) / test(438) / build 全绿。

### 步骤 4：lib/shared 收敛与类型漂移修复 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 4.1 | `NativePlayHistoryRecord` 双声明合并（native-history.ts 改从 messages 导入） | ✅ |
| 4.2 | `transcode-store.ts` 移出 shared 归入 content 域（含测试，git mv 保历史） | ✅ |
| 4.3 | `parseJsonText` 统一收口 shared/utils；上下文失效检测统一 `isContextInvalidated`（videoThumbnail 复用） | ✅ |
| 4.4 | `videoThumbnail.ts` 拆分 | ⏭️ 评估后降级：缓存层与生成策略深度交织（模块级状态互引），完全拆分风险>收益，沿用 P5 结论 |
| 4.5 | `player.ts` `createArtplayer` 396 行拆分 | ⏭️ 评估后降级：组合根装配本质是 attach 闭包编排，强行拆分引入间接层；改为下沉 `formatFileSize` 至 shared，消除三层透传 |

> 步骤 4 全量验证：typecheck / test(438) 全绿。

### 步骤 5：测试补强（H 级盲区） ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 5.1 | `background/index.ts` 安全路由测试（isTrustedSender / MAIN world URL 白名单 / cookie 域名，10 个） | ✅ |
| 5.2 | `background/transcode.ts` 状态机测试（queued/completed/no_task/autoFallback/failed，7 个） | ✅ |
| 5.3 | crypto/rsa 往返测试 | ⏭️ 降级：实测 512-bit RSA 模幂在测试环境单次往返 >60s，不适合单元测试（性能瓶颈而非逻辑缺陷），移除并记录 |
| 5.4 | `main-world.ts` 模式回退决策抽 `resolveVodFetchModeSteps` 纯函数 + 测试（5 个） | ✅ |
| 5.5 | `native-history.ts` `parseNativeRecord`（6 个）+ `transcode-api.ts` 请求构造（5 个） | ✅ |
| 5.6 | content 缩放/拖拽物理纯函数测试（步骤 2 顺带，viewer-transform 14 个） | ✅ |

> 步骤 5 全量验证：typecheck / lint(0 error) / test(**471**) / build 全绿。

### 步骤 6：全量验证 + 收尾 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 6.1 | 全量 typecheck / lint(0 error) / test(**471**) / build 回归 | ✅ |
| 6.2 | 对照 `.rules/迭代否决项.md` 复查，更新未做清单 | ✅ |
| 6.3 | 删除本任务产生的临时文件（temp/） | ✅ 无残留 |

---

## 未做清单（低优先/暂缓，遇新证据再评估）

| 项 | 说明 |
|---|---|
| L7 | unarchive 目录 limit 业务边界 |
| L8/L12 | 看图器/侧边栏 doc 级 keydown 常驻监听（observer 统一时评估） |
| L10 | 后台缓存 Map 惰性清理（版本清扫已做，age 淘汰待评估） |
| L15 | messages 反向 import（已定论：合理，不改） |
| L16 | 双 utils 命名（core 侧改 dom-utils） |
| L20 | main-world 命名（暂缓） |
| lint any | 既有宽松风格，0 错误，不强制清零 |
| 3.4 看图器交互调整 | 用户暂缓，等反馈 |
| content 6 处 L 级资源残留 | sidebar observer / refreshTimers / frame load / preload timer / lightbox overlay 常驻 / preview iframe 清理死角（无实害，页面级常驻可接受） |
| player 5 处 L 级 timer 残留 | hover-preview once / subtitle setTimeout / move-dialog searchTimer / button click 监听 / player-volume 监听（均有 destroy guard） |
| telemetry 失败计数只增不清 | 上限低，暂缓 |
| crypto/rsa 往返测试 | 512-bit RSA 模幂测试环境 >60s/次，性能瓶颈非逻辑缺陷，暂缓 |
| 控件构建结构性重复 | 5 个 artplayer control 工厂化（收益一般，暂缓） |

---

## P5 历史（已完成，2026-08-14）

- **P5 审计结论**：2 个真实缺陷（player destroy 缺 4 控制器 → 连播 setInterval 泄漏；预载器 inflight 并发上限形同虚设）+ 重复 × 大量 + 大文件 × 3（overlay/videoThumbnail/transcode）。
- **步骤 0**：destroy 补齐 / 预载并发修复 / 转码 observer 收敛（`1ac2702`）。
- **步骤 1**：overlay 类型抽 `overlay-types.ts` 解 2 类型环 / native 交互下沉（`0d1e254`）。
- **步骤 2**：debug / safePlay / toast 评估降级 / DOM 解析 / mapWithConcurrency / MAIN world fetch / updateArtplayerControl 基建重复收敛（`0ba5f34`/`66c5721`/`244f1e2`）。
- **步骤 3**：overlay 可见性 / hover 策略 / 偏好存储 / 转码拆 transcode-api+state / videoThumbnail 导出纯函数保持单文件（`23e026c`/`909d7bd`/`6babeda`）。
- **步骤 4**：转码/解码/帧重组/缩略图纯函数补测（`d991751`）。
- **步骤 5**：死导出/死 barrel/空实现清理（`e97caa5`），全量 426 测试绿。
- **成果**：426 测试，lint 0 error，build 全绿。

## P4 历史（已完成，2026-08-13 ~ 08-14）

- P0-P3：稳定/性能/深度审计修复（285 测试基线）。
- v1.8.9 发布；版号定 1.9.0（停留较久，架构筑基载体）。
- P4 步骤 1：player.ts 1879→931 行，拆 7 控制器（`3fa1820`~`8209a94`）。
- P4 步骤 2：共享 toast / move-dialog 样式收编 / 图标惰性化。
- P4 步骤 3：看图器原创为主，邻居预载模块化 + 测试补强。
- P4 收尾：死代码清理、photoswipe 卸载（`c093e74`）、4 测试盲区补齐（`1a961f8`）。
- **成果**：388 测试，lint 0 error，build 全绿。

---

## 收尾约定

- 未做项不是永久关闭：深度审计或用户反馈提供新证据时重新评估。
- 每步完成后：typecheck / lint / 相关测试 / build 全绿再进入下一步。
- P6 为架构收口第二轮，冻结新功能；达到「架构优质、基础稳定」标准后评估 1.9.0 发布。
- **1.9.0 版号会停留较久**，作为架构筑基与基础稳定的载体。
