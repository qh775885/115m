# 迭代计划

> 目标：1.9.0 前做到又快又稳、低开销高性能，功能增强暂停，遇到实际问题再说。
> 测试：改到哪补到哪，顺手补关键测试。
> 已否决项见 `.rules/迭代否决项.md`，规划前按需对照，避免重复提出。

## 阶段状态

- **P0/P1/P2 稳定与性能迭代已全部完成（2026-08-13）**，质量基线：typecheck / lint / 266 测试 / pnpm build 全绿。
- **P3 深度审计完成（2026-08-13）**，产出六大维度审计报告，问题分级如下。
- **P3 修复已完成（2026-08-13）**：H 级 4 项 + M 级 17 项 + L1-L3 全部修复，新增测试 19 个。基线：typecheck / lint(0 error) / 285 测试 / build 全绿。

---

## P3 审计结论（已交付审阅，未修改任何代码）

### 审计基线
- 架构：无循环依赖，shared/lib 基础层不被上层反向依赖，MAIN world 边界清晰。
- 资源：确凿活跃泄漏 0 项，剩余风险集中在持久化层无清理 + background 侧少量 fetch 缺超时。
- 消息：19 个请求类型收发闭环，无孤儿消息；发现 1 僵尸消息 + 1 响应类型缺口。
- 构建：typecheck / 266 测试 / build 全绿；lint 145 warnings（0 errors）。

### 问题分级

#### P3-H 高优先级（正确性/性能热点）

| # | 模块 | 问题 |
|---|------|------|
| H1 | `clipper/DecoderFlow.ts:167-264` | waitForFrame 主循环忙等空转（1.5s 宽限期 + 5s 超时纯空转），悬停预览连续 seek 持续耗 CPU |
| H2 | `player/core/hover-preview-session.ts:247-304` | loadThumbnails 无 generation 失效保护，HLS duration 变化后过期 covers 覆盖新状态 |
| H3 | `player/core/hover-preview-session.ts:518-577` | loadPreciseCover 同样无 generation，过期精确帧写回新状态 |
| H4 | `player/core/move-dialog.ts:469-484` | 接口失败被吞 → 空目录误判，用户无法区分网络失败与真空目录 |

#### P3-M 中优先级

| # | 模块 | 问题 |
|---|------|------|
| M1 | `player.ts:349-352` | 快速切集时 blob URL 竞态泄漏（未 revoke） |
| M2 | `player.ts:614-616` | 不支持 HLS 分支漏 failSwitchUrl，switchUrl 永久挂起 |
| M3 | `player.ts:1657/1760-1763` | isNativeVideo 与 currentPlaybackType 双状态源可能不同步 |
| M4 | `clipper/DecoderFlow.ts:548-578` | decode() 同步异常被吞，空转至超时且难排查 |
| M5 | `clipper/DecoderFlow.ts:61` | samplesProcessed 无界增长（调试残留） |
| M6 | `hover-preview-session.ts:55` | preciseCovers Map 无上限，dataURL 长时间积累 |
| M7 | `media-wall-images.ts:298-303` | 切图预载 2 张原图无界，快速翻页网络/内存风暴 |
| M8 | `media-wall-images.ts:244-251+339-367` | 双 RAF 循环同时写 transform 可能抖动 |
| M9 | `unarchive-actions.ts:163-172` | 批量解压最长 15 分钟持续轮询，单文件与批量可并发 |
| M10 | `overlay.ts:769-771` | mousemove 高频 setVisible 无节流 |
| M11 | 消息契约 | DOWNLOAD 僵尸消息（无发送方），契约与 handler 未同步删除 |
| M12 | 消息契约 | FETCH_SUBTITLES 未收录 RuntimeMessageResponseMap，响应类型为 unknown |
| M13 | 资源泄漏 | `native-history.ts:50,117`、`media-info.ts:70` fetch 无超时 |
| M14 | 资源泄漏 | `transcode-store.ts:75-80`、`videoThumbnail.ts` 存储只增不删，长会话可累积 |
| M15 | 架构 | 双套 runtime 消息模块重复（content/player），漂移风险高 |
| M16 | 架构 | `video-page.content.ts:27-71` 入口堆积 45 行业务逻辑 |
| M17 | 架构 | `createScrollStopDetector` 死代码 + preview.ts 重复实现 |

#### P3-L 低优先级

| # | 模块 | 问题 |
|---|------|------|
| L1 | `player.ts:1442-1449` | refreshBreadcrumbs 无 catch 的 void promise |
| L2 | `player.ts:1372-1384` | 自动连播未传 keepPlaylistOpen，与 next 模式不一致 |
| L3 | `player.ts:582-585` | 原画加载成功前已写偏好 |
| L4 | `move-dialog.ts:360-363` | 搜索 debounce 定时器关闭后未清理 |
| L5 | `hover-preview-session.ts:484-491` | prioritize 分支死代码 |
| L6 | `overlay.ts:343-351` | toast 内层定时器未收口 |
| L7 | `unarchive-actions.ts:65` | limit=1150 可能漏判重名 |
| L8 | `media-wall-images.ts:650-667` | doc keydown 监听永久存在 |
| L9 | 资源泄漏 | `history.ts:54` lastPlayHistoryWriteAt 只增不删；`history.ts:93-175` 偏好 map 无清理 |
| L10 | 资源泄漏 | `transcode.ts:45-52` 三个 Map 惰性 TTL；`media-info.ts:21` playlistCache 惰性 |
| L11 | 资源泄漏 | `subtitles.ts:84`、`videoThumbnail.ts:217`、`file-actions.ts:60` fetch 无超时 |
| L12 | 资源泄漏 | `sidebar.ts:275`、`media-wall-images.ts:650` 页面级单例监听无显式移除 |
| L13 | 资源泄漏 | `player.ts:1339` clearTimeout 清理 interval（瑕疵） |
| L14 | 资源泄漏 | `history.ts:263` 死代码 loadPlayHistory 内含无清理定时器 |
| L15 | 架构 | `shared/messages.ts:1-2` 反向 type import lib（编译期擦除） |
| L16 | 架构 | 双 `utils.ts` 同名混淆（内容零重叠） |
| L17 | 架构 | `move-dialog.ts:764-766` 私有 escapeHtml 冗余包装 |
| L18 | 架构 | `shared/icons.ts` 模块级 DOM 注入副作用 |
| L19 | 架构 | `wait`（shared/utils）与 `promiseDelay`（lib/promise）重复 |
| L20 | 架构 | `platform/115/main-world.ts` 命名误导（实际运行于 SW） |
| L21 | 测试盲区 | 67 个无测试源码，高风险缺测：player.ts / move-dialog.ts / DecoderFlow.ts / hover-preview-session.ts / unarchive-actions.ts / hover-preview.ts / media-wall-folders.ts / sidebar.ts |
| L22 | 构建 | lint 145 warnings 未清零（多为 any 与未用变量） |

---

## P3 修复执行计划（待审计结论确认后按步执行）

> 规则：改到哪补到哪；每项完成后跑 typecheck/lint/相关测试/build。按依赖顺序排序。

### 步骤 1：正确性修复（H 级）
1. H4 move-dialog 空目录误判 → 区分网络失败与空目录，失败态提示。
2. H2+H3 hover-preview-session 增加 generation 失效保护（duration/切集时递增，在途回调比对）。
3. H1 DecoderFlow waitForFrame 空转优化（等帧事件驱动/条件变量替代忙等，或至少降低轮询频率）。

### 步骤 2：播放器核心修复（M 级）
4. M1 blob URL 竞态 revoke。
5. M2 不支持 HLS 分支补 failSwitchUrl。
6. M3 isNativeVideo 与 currentPlaybackType 单一状态源。
7. L1-L3 player.ts 杂项（refreshBreadcrumbs catch / 连播 keepPlaylistOpen / 原画偏好时机）。

### 步骤 3：资源与性能修复（M 级）
8. M4+M5 DecoderFlow decode 异常上抛 + samplesProcessed 收敛。
9. M6 preciseCovers Map 上限淘汰。
10. M7 media-wall-images 预载节流/取消。
11. M8 双 RAF 循环合并。
12. M10 overlay mousemove 节流。
13. M13 fetch 超时补齐（native-history / media-info / subtitles / videoThumbnail / file-actions）。
14. M14 存储清理策略（transcode-store 上限淘汰、videoThumbnail 按时间淘汰）。
15. L9-L13 资源杂项清理。

### 步骤 4：架构与契约修复（M 级）
16. M11 删除 DOWNLOAD 僵尸消息契约。
17. M12 FETCH_SUBTITLES 补响应映射 + subtitle-controller normalize 对齐。
18. M15 双 runtime 模块合并为 shared 单源。
19. M16 入口业务逻辑下沉 video-page.content。
20. M17 删除 createScrollStopDetector 死代码。
21. L15-L20 架构杂项。

### 步骤 5：测试补强（M 级）
22. 为步骤 1-4 改动补充针对性测试。
23. 优先补测无测试高危模块：hover-preview-session / DecoderFlow（waitForFrame 忙等回归） / move-dialog（目录加载失败分支） / unarchive-actions（批量轮询）。
24. 收尾：lint warnings 清理（可接受范围内）+ 全量回归 + 迭代收尾提交。

---

## P3 修复执行记录（2026-08-13 已完成）

### 步骤 1：正确性修复（H 级）✅
1. **H4** move-dialog-api `apiFetchFolders` 返回 `error` 字段区分「网络失败/接口异常/解析失败」；`loadFolder` 失败渲染错误提示 + 重试按钮，不再误判为空目录。
2. **H2+H3** hover-preview-session 新增 `thumbnailGeneration` 计数器，`resetThumbnailState` 递增；`loadThumbnails`/`runBackgroundRefinement`/`loadPreciseCover` await 后比对 generation，失效丢弃；`loadPreciseCover` finally 在 generation 失效时不消费新队列。
3. **H1** DecoderFlow 空闲轮询 `promiseDelay(0)`→`IDLE_POLL_MS(30)`；找到帧且 `_hasPendingWork()` 为 false 时立即返回，不再空等宽限期。

### 步骤 2：播放器核心修复 ✅
4. **M1** `initHls` seq 失配时 revoke 临时 blob URL。
5. **M2** 不支持 HLS 分支补 `failSwitchUrl`，打破 switchUrl 永久挂起。
6. **M3** `applyPlaybackStatePatch` 更新 `isNativeVideo` 时同步 `currentPlaybackType`，消除双状态源不同步。
7. **L1** MOVE_REFRESHED 刷新面包屑加 catch；**L2** 自动连播 interval 传 keepPlaylistOpen；**L3** 原画偏好移到加载成功后写入。

### 步骤 3：资源与性能修复 ✅
8. **M4** `_processSampleQueue` decode 同步异常置 error + stop，交由 waitForFrame 上抛。
9. **M5** `samplesProcessed` 收敛为 `MAX_KEPT_SAMPLES(64)` 环形。
10. **M6** `preciseCovers` 超 `MAX_PRECISE_COVERS(200)` 淘汰最旧 bucket。
11. **M7** `preloadNeighbors` 加 `preloadVersion` 失效保护 + 近邻优先错峰（index*120ms）。
12. **M8** `finishDrag` 取消 drag RAF，settle 动画独占收敛，消除双循环抖动。
13. **M10** overlay `showTemporarily` 50ms 节流。
14. **M13** native-history（get/set）、media-info 字幕 fallback、subtitles、videoThumbnail 补 15s fetch 超时；file-actions delete 补 AbortSignal.timeout。
15. **M14** transcode-store 加 `MAX_STORE_RECORDS(300)` 按 updatedAt 淘汰；videoThumbnail 已有三层防护评估为低收益不做（写入否决）。

### 步骤 4：架构与契约修复 ✅
16. **M11** 删除 DOWNLOAD 僵尸消息（契约 + handler + assertDownloadUrl/DOWNLOAD_ALLOWED_HOSTS 死代码）。
17. **M12** 新增 `FetchSubtitlesResponse` 并入 `RuntimeMessageResponseMap`。
18. **M15** 新建 `shared/runtime-utils.ts` 单源共享 isContextInvalidated/showContextInvalidatedTip/formatRuntimeMessage/getRuntimeApi/canUseRuntimeMessaging；content 与 player runtime 复用，调用方零改动。
19. **M16** 画面点击播放/暂停逻辑下沉 `content/core/video-play-pause-binder.ts`，入口同步导入保持时序。
20. **M17** 删除 `createScrollStopDetector` 死代码。

### 步骤 5：测试补强 ✅
21. 新增测试 19 个：hover-preview-session（4，generation 失效 + 淘汰）、DecoderFlow（2，忙等提前返回）、move-dialog-api（11，错误分支）、transcode-store（+1，上限淘汰）。
22. 全量回归：typecheck / lint(0 error, 149 warning) / **285 测试** / build 全绿。

### 遗留（低优先，未纳入本轮）
- **L4-L20 低优先级项**（debounce 清理、死代码 loadPlayHistory、命名混淆等）：收益低、风险可控，待实际反馈再处理。
- **player.ts 拆分**（1865 行建议拆分 5-6 模块）：纯架构整洁，本轮未动，避免大改引入回归。
