# 迭代计划

> 目标：把项目做成**架构优质、干净整洁、健康可持续、稳定高性能**。核心功能：**播放器 + 看图器**。
> 发展驱动：深度审计 + 用户反馈双轨（见 `.rules/迭代否决项.md` 第四节）。
> 测试：改到哪补到哪，杂项与测试穿插在每步中做。

---

## 📊 当前进度总览（2026-08-14 P6 完成）

| 阶段 | 状态 | 说明 |
|---|---|---|
| P0-P5 已完成（v1.8.9 发布 → 架构筑基 → P5 深化） | ✅ | 426 测试基线 |
| v1.9.0 用户实测 | 🔄 | 无新增问题反馈 |
| **P6 全局架构审计** | ✅ | 六步完成；期间发现并修复「无损画质丢失」（MAIN world 注入闭包序列化失效） |

**当前累计**：测试 **471 个**（P5 基线 426，+45）；typecheck / lint(0 error) / build 全绿。

---

## P6 审计结论（2026-08-14，第六轮）

**判断：架构已进入冲刺期，无新增真实功能缺陷。** P5 的 2 个缺陷（destroy 泄漏/预载并发）已修复未复发。剩余债集中在三类：

1. **重复**：`getItemTitle/getItemName` 同体、folders/images 选择同步复制、音量偏好双写、m3u8 回调三连、时间/名称/cid 多变体、parseJson/上下文失效检测双实现。
2. **大文件**：`preview.ts`(875)/`media-wall-images.ts` lightbox 单闭包 618 行/`videoThumbnail.ts`(617)/`player.ts` createArtplayer 396 行。
3. **死代码 + 测试盲区**：`TRANSCODE_FRAME_READY` 死协议整链、`setDownloadCookie`/`formatVttTime` 零引用；background 安全路由/转码状态机/crypto 链路/main-world 回退无测试。

**分层健康度**：依赖无环 ✅ / 消息契约 18 种对应 ✅ / 无 H 级泄漏 ✅ / 重复收敛接近尾声 / 可测性大幅提升。

**核心策略**：先清死代码 → 重复收口 → content 大文件拆分 → player 封装修复 → lib/shared 收敛 → 补测试盲区。

---

## P6 执行成果（已完成）

| 步骤 | 成果 |
|---|---|
| 0 死代码 | 删除 `TRANSCODE_FRAME_READY` 整链协议、`setDownloadCookie`、`formatVttTime`、`forwardNativeContextMenu` 死管道、debug 挂载修复 |
| 1 重复收敛 | `getItemTitle/getItemName` 合并、选择同步抽 `createWallSelectionButton/syncWallSelectionState`、魔法数命名常量、`getItemParentId` 统一、9 处硬编码 URL 收敛 |
| 2 大文件拆分 | `preview.ts` 875→~360 行，拆 `transcode-button`/`cover-lightbox`/`observer-registry` + `viewer-transform` 物理纯函数（14 测试） |
| 3 player 修复 | `resolveQualityPlayback` 消原画三连、音量单入口、私有越权修复、`readOverlayMetaFromQuery` 下沉、`resetVideoProgress`/`formatCompactTime`/`buildAudioTrackLabel` 收敛 |
| 4 lib/shared | `transcode-store` 归 content 域、`NativePlayHistoryRecord` 合并、`parseJsonText`/`isContextInvalidated` 统一、`formatFileSize` 下沉消三层透传 |
| 5 测试补强 | background 安全路由(10)/转码状态机(7)/main-world 回退(5)/native-history(6)/transcode-api(5)；crypto 往返降级 |
| 修复 | 无损画质丢失：MAIN world 注入函数闭包变量 `withHeaders` 序列化失效 → 改显式参数随 args 传递 |

**降级项**：`videoThumbnail` 拆分（缓存/策略深度交织）、`createArtplayer` 拆分（组合根本质）、crypto 往返测试（RSA 测试环境 >60s/次）。

**提交**：`76d2ea3`~`e1d5729` 共 7 笔（重构×5 / 修复×1 / 文档×2）。

---

## 未做清单（低优先/暂缓，遇新证据再评估）

| 项 | 说明 |
|---|---|
| L7 | unarchive 目录 limit 业务边界 |
| L8/L12 | 看图器/侧边栏 doc 级 keydown 常驻监听（observer 统一时评估） |
| L10 | 后台缓存 Map 惰性清理（版本清扫已做，age 淘汰待评估） |
| L15 | messages 反向 import（已定论：合理，不改） |
| L16 | 双 utils 命名（core 侧改 dom-utils） |
| L20 | main-world 命名（暂缓） |
| lint any | 既有宽松风格，0 错误，不强制清零 |
| 3.4 看图器交互调整 | 用户暂缓，等反馈 |
| content 6 处 L 级资源残留 | sidebar observer / refreshTimers / frame load / preload timer / lightbox overlay 常驻 / preview iframe 清理死角（无实害，页面级常驻可接受） |
| player 5 处 L 级 timer 残留 | hover-preview once / subtitle setTimeout / move-dialog searchTimer / button click 监听 / player-volume 监听（均有 destroy guard） |
| telemetry 失败计数只增不清 | 上限低，暂缓 |
| crypto/rsa 往返测试 | 512-bit RSA 模幂测试环境 >60s/次，性能瓶颈非逻辑缺陷，暂缓 |
| 控件构建结构性重复 | 5 个 artplayer control 工厂化（收益一般，暂缓） |

---

## P5 历史（已完成，2026-08-14）

- **审计结论**：2 个真实缺陷（player destroy 缺 4 控制器 → 连播 setInterval 泄漏；预载器 inflight 并发上限形同虚设）+ 重复 × 大量 + 大文件 × 3（overlay/videoThumbnail/transcode）。
- **步骤 0**：destroy 补齐 / 预载并发修复 / 转码 observer 收敛（`1ac2702`）。
- **步骤 1**：overlay 类型抽 `overlay-types.ts` 解 2 类型环 / native 交互下沉（`0d1e254`）。
- **步骤 2**：debug / safePlay / toast 评估降级 / DOM 解析 / mapWithConcurrency / MAIN world fetch / updateArtplayerControl 基建重复收敛（`0ba5f34`/`66c5721`/`244f1e2`）。
- **步骤 3**：overlay 可见性 / hover 策略 / 偏好存储 / 转码拆 transcode-api+state / videoThumbnail 导出纯函数保持单文件（`23e026c`/`909d7bd`/`6babeda`）。
- **步骤 4**：转码/解码/帧重组/缩略图纯函数补测（`d991751`）。
- **步骤 5**：死导出/死 barrel/空实现清理（`e97caa5`），全量 426 测试绿。
- **成果**：426 测试，lint 0 error，build 全绿。

## P4 历史（已完成，2026-08-13 ~ 08-14）

- P0-P3：稳定/性能/深度审计修复（285 测试基线）。
- v1.8.9 发布；版号定 1.9.0（停留较久，架构筑基载体）。
- P4 步骤 1：player.ts 1879→931 行，拆 7 控制器（`3fa1820`~`8209a94`）。
- P4 步骤 2：共享 toast / move-dialog 样式收编 / 图标惰性化。
- P4 步骤 3：看图器原创为主，邻居预载模块化 + 测试补强。
- P4 收尾：死代码清理、photoswipe 卸载（`c093e74`）、4 测试盲区补齐（`1a961f8`）。
- **成果**：388 测试，lint 0 error，build 全绿。

---

## 收尾约定

- 未做项不是永久关闭：深度审计或用户反馈提供新证据时重新评估。
- 每步完成后：typecheck / lint / 相关测试 / build 全绿再进入下一步。
- **当前基调（2026-08-14 定）**：
  - 项目定位 = **维持健康稳定**，不做主动重构、不扩功能，等待用户实测反馈。
  - 架构审计改为**按需触发**：有具体痛点、用户反馈或引入大改动时才做针对性审计，不再固定轮次。
  - 日常健康度看门狗：**471 测试全绿 + lint 0 error + build 通过** 即视为健康，达标不多动。
  - **1.9.0 发布由用户决定并标记**；版号停留期间以基础稳定为唯一目标。
