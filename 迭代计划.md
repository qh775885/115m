# 迭代计划

> 目标：1.9.0 前做到又快又稳、低开销高性能，功能增强暂停，遇到实际问题再说。
> 测试：改到哪补到哪，顺手补关键测试。

## 已完成（2026-08-12，均已提交）

- P0-1~6：进度节流保存不丢、稳态 HLS fatal 自愈、弱网超时链全覆盖、DecoderFlow 读块互斥、li 复用播放重绑、下载拦截幂等。
- P1-7~8：cache-sweeper 只读版本 key、切无损源销毁旧 hls。
- P1-11~12：封面失败结果不缓存可重试、播放器预置缩略图源；master 文本 30s 缓存 + 音轨水化复用。
- 质量：typecheck / lint / 241 测试 / pnpm build 全绿。

## 已完成（2026-08-13）

- P2-1：设置控件 MutationObserver 拆解挂载（teardownControl）+ selector Set 卸载解绑（unbindClickSelectorBehavior，MediaTrack/Subtitle/Settings 共用）；音轨重建 `once` 残留改为带 hlsInitSeq token 的单一 on 监听，失败/切集自动摘除。
- P2-2：封面/单点/时间轴缓存改为字节预算 LRU（`ByteBudgetCache`，64/32/64 MB），dataURL 按 UTF-16 估算；单点封面 in-flight 去重与结果缓存拆分；时间轴按 pickCode 封顶 200 条、读写两端均匀截断，杜绝单调增长与全量读写 O(N²)。
- 质量：typecheck / lint / 252 测试（新增 ByteBudgetCache 4 例 + videoThumbnail 时间轴封顶 4 例）/ pnpm build 全绿。

## 明日待办（按优先级）

### P2-3 常驻浪费（真实，修复明确）

4. native-monitor 暂停时 audio/video 探测无限自我重排（`native-playback-monitor.ts:318-340`）。→ pause 停探测、play 重排。
5. 字幕 rAF 渲染循环每帧无变化也写 DOM（`subtitle-manager.ts:254`）。→ 缓存上次 cue，仅变化时写。
6. 转码状态轮询（content 每 15s）每次串行 3~5 个请求、无去重无缓存（`transcode.ts:198+`）。→ per-pickCode 短缓存/in-flight 合并。

### P2-4 列表页稳定与响应

7. `FETCH_PLAYLIST` 每次全量拉 1150 条、MB 级双重序列化、无缓存（`media-info.ts:89`）。→ 按 cid 短 TTL 内存缓存。
8. wangpan iframe 换文档后旧 Document 注册表不清理（`home.ts`、`preview.ts` Map 键累积）。→ 绑新 doc 前先 unbind 旧 doc，注册表改 WeakMap。
9. 扫描管线无模块化 try/catch，单坏点静默劣化整批（`home.ts:102-157`）。→ scanAndRender 内按模块 try/catch。

## 已排除（不再做，避免重复提起）

- P1-9 悬停队列：已有去重 + `hoverRequestVersion` 失效保护 + destroy 清空，改动风险大于收益。
- P1-10 预热数量收敛：实际开销低于审计预期（180 张仅 >180min 视频），收敛涉体验属行为变化，不做。
- P1-13 home DOM 热路径重构：已有 rAF 合并 + media-wall 短路 + pickCode 去重三层防护，重构风险高，留待实际卡顿反馈。
- P0-2 HLS 卡顿看门狗：已有 fatal 自愈兜底，降级策略待确认且收益不确定，不做。
- P2 其余杂项：转码 cooldown Map、`waitForTabComplete` 竞态、telemetry 丢批、logger 热路径拼串、NALU 拷贝、BoundedCache LRU、HlsIO destroy 不 abort、灯箱预载、滚动高频 sessionStorage、音量拖动逐次写、overlay 列表分片、孤儿隐藏标签、scannedPickCodes 全清——收益过低或边缘场景，不做。
