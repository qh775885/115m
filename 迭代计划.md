# 迭代计划

> 目标：把项目做成**架构优质、干净整洁、健康可持续、稳定高性能**。核心功能：**播放器 + 看图器**。
> 发展驱动：深度审计 + 用户反馈双轨（见 `.rules/迭代否决项.md` 第四节）。
> 测试：改到哪补到哪，杂项与测试穿插在每步中做。

---

## 📊 当前进度总览（2026-08-14 P5 完成）

| 阶段 | 状态 | 说明 |
|---|---|---|
| P0-P4 已完成（v1.8.9 发布、player.ts 拆分、UI 基建、看图器、收口） | ✅ | 388 测试基线，见下方 P4 历史 |
| v1.9.0 用户实测 | 🔄 | 无新增问题反馈，可进入 P5 |
| **P5 步骤 0：正确性缺陷修复** | ✅ | destroy 补齐 / 预载并发 / observer 收敛（`1ac2702`） |
| **P5 步骤 1：依赖倒置** | ✅ | overlay 类型抽离 / native 操作下沉（`0d1e254`） |
| **P5 步骤 2：基建重复收敛** | ✅ | debug/toast/DOM 解析/请求封装（`0ba5f34`/`66c5721`/`244f1e2`；toast 评估降级） |
| **P5 步骤 3：单体拆分** | ✅ | overlay 可见性/悬停策略/偏好存储/转码/缩略图（`23e026c`/`909d7bd`/`6babeda`） |
| **P5 步骤 4：测试补强** | ✅ | 转码/解码/帧重组/缩略图纯函数（`d991751`） |
| **P5 步骤 5：死代码清理 + 全量验证** | ✅ | 死导出/死 barrel/空实现清理（`e97caa5`） |

**当前累计**：测试 **426 个**（P4 基线 388，+38）；typecheck / lint(0 error) / build 全绿。

---

## P5 全局架构审计结论（2026-08-14）

### 判断：架构骨架健康，债集中在「重复」与「三个大文件」

三域并行深度审计（content / player / lib+platform+shared+background）完成。**分层无环、消息契约完备、销毁意识已成型**，正确方向。剩余问题分两类：

1. **真实缺陷（2 处确认）**：
   - `player.ts destroy()` 遗漏 4 个控制器销毁（playlist/actions/uiControls/quality）→ 连播 `autoNextTimer` setInterval 泄漏，destroy 后仍会触发 `navigateToVideo`。
   - 预载器 `media-wall-preload.ts:50` `inflight -= 1` 立即减回 + `acquire/release` 无调用者 → **并发上限形同虚设**，快速翻页瞬时发起多张图片加载。

2. **架构债（重复 × 大量 + 大文件 × 3）**：
   - **复制粘贴式重复**：`debugLogToPage` 三份、`safePlay` 三份、debug 门控 8 处、三套 toast、原生交互模拟 3 处、文件属性 DOM 解析 3 处、`mapWithConcurrency` 2 份、main-world 注入 fetch 3 份、`updateArtplayerControl` 2 份。
   - **领域类型嵌 UI**：`overlay.ts` 的 4 个领域类型被 11 模块引用，形成 2 个类型级循环。
   - **大文件**：`overlay.ts` 805 行四子系统混居、`videoThumbnail.ts` 636 行五职责、`transcode.ts` 568 行三职责。
   - **可测性缺口**：转码状态机、DecoderFlow 分支、demuxer、hover-preview 策略函数、overlay 可见性状态机均无测试。

### 分层健康度评估表

| 维度 | 结论 |
|---|---|
| 依赖方向 | ✅ 无环 DAG；lib → shared/platform → background → content/player |
| 消息契约 | ✅ 收敛 messages.ts；转码双通道广播是亮点 |
| 资源管理 | ⚠️ destroy 补齐（H1）+ 后台缓存懒清理 + 部分 observer 常驻 |
| 单体 | ⚠️ player.ts 已好；overlay/videoThumbnail/transcode 待拆 |
| 重复 | 🔴 最多的问题类，见步骤 2 |
| 可测性 | ⚠️ 纯函数抽取方向正确，但核心编排仍不可测 |

### 核心策略

1. **正确性优先**：先修 2 个确认缺陷（destroy 泄漏 / 预载并发）。
2. **解环再拆**：先抽 overlay 类型、下沉 native 操作，再拆大文件。
3. **重复收口**：基建（debug/toast/DOM 解析/请求）统一到 shared，消除复制粘贴。
4. **抽纯函数 + 补测试**：每个大文件拆出可测纯函数域。
5. **穿插执行**：每步带测试 + 顺手清理 L 级杂项。

---

## P5 执行步骤

### 步骤 0：正确性缺陷修复 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 0.1 | `player.ts destroy()` 补齐 playlist/actions/uiControls/quality 四控制器销毁 | ✅ |
| 0.2 | 预载器并发修复：`acquire/release` 正确接线，`inflight` 不再立即减回 | ✅ |
| 0.3 | 转码按钮 MutationObserver 膨胀：改为 doc 级共享 registry | ✅ |

### 步骤 1：依赖倒置 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 1.1 | overlay 领域类型抽 `overlay-types.ts`，解 2 类型环 | ✅ |
| 1.2 | 原生交互函数下沉 `native-interact.ts` | ✅ |

### 步骤 2：基建重复收敛 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 2.1 | player 侧 debug 统一 `debug.ts` | ✅ |
| 2.2 | `safePlay` 统一 `media.ts` | ✅ |
| 2.3 | toast 三套统一 | ⏭️ 评估后降级：unarchive/move-dialog 是特殊用途，shared toast 能力不匹配，避免行为变化 |
| 2.4 | 原生交互模拟抽公共工具（forwardNativeContextMenu 下沉） | ✅ |
| 2.5 | 文件属性 DOM 解析收口 native-dom | ✅ |
| 2.6 | `mapWithConcurrency` 提 shared | ✅ |
| 2.7 | main-world 注入 fetch 工厂化 | ✅ |
| 2.8 | `updateArtplayerControl` 统一 | ✅ |

### 步骤 3：单体拆分 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 3.1 | overlay 可见性决策抽 `overlay-visibility.ts` 纯函数 | ✅ |
| 3.2 | hover-preview 策略抽 `hover-preview-policy.ts` | ✅ |
| 3.3 | history 偏好存储抽 `storageMap` 工厂 | ✅ |
| 3.4 | transcode 拆 `transcode-api.ts` + `transcode-state.ts` | ✅ |
| 3.5 | videoThumbnail 拆分 | ⏭️ 评估后降级：渲染与缓存深度交织，完全拆分风险>收益；导出纯函数 + 补测试保持单文件 |

### 步骤 4：测试补强 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 4.1 | 转码纯逻辑（buildQueuedResponse/缓存 TTL/冷却）补测 | ✅ |
| 4.2 | DecoderFlow 超时分支 + demuxer 帧重组补测 | ✅ |
| 4.3 | hover-preview-policy 阶梯边界测试 | ✅（步骤 3 顺带） |
| 4.4 | overlay 可见性决策测试 | ✅（步骤 3 顺带） |

### 步骤 5：死代码清理 + 全量验证 ✅ 已完成

| 子项 | 内容 | 状态 |
|---|---|---|
| 5.1 | 死导出清理（qualityNumMap/VideoSource/icons 8 图标/timebase 无引用/hasFrame/decodedFramesTimes） | ✅ |
| 5.2 | 死 barrel `lib/index.ts` 删除 | ✅ |
| 5.3 | 空实现/死监听（clearLogs no-op 链/video:play 空监听/window.playerManager debug 保护） | ✅ |
| 5.4 | 全量 typecheck / lint / test(426) / build 回归 | ✅ |

---

## 未做清单（低优先/暂缓，遇新证据再评估）

| 项 | 说明 |
|---|---|
| L7 | unarchive 目录 limit 业务边界 |
| L8/L12 | 看图器/侧边栏 doc 级 keydown 常驻监听（observer 统一时评估） |
| L10 | 后台缓存 Map 惰性清理（onSuspend 周期清扫，P5 步骤 3.4 顺带） |
| L15 | messages 反向 import（已定论：合理，不改） |
| L16 | 双 utils 命名（core 侧改 dom-utils，P5 步骤 2 顺带） |
| L20 | main-world 命名（暂缓） |
| lint 145 any | 既有宽松风格，0 错误，不强制清零 |
| 3.4 看图器交互调整 | 用户暂缓，等反馈 |

---

## P4 历史（已完成，2026-08-13 ~ 08-14）

- P0-P3：稳定/性能/深度审计修复（285 测试基线）。
- v1.8.9 发布；版号定 1.9.0（停留较久，架构筑基载体）。
- P4 步骤 1：player.ts 1879→931 行，拆 7 控制器（`3fa1820`~`8209a94`）。
- P4 步骤 2：共享 toast / move-dialog 样式收编 / 图标惰性化。
- P4 步骤 3：看图器原创为主，邻居预载模块化 + 测试补强。
- P4 收尾：死代码清理、photoswipe 卸载（`c093e74`）、4 测试盲区补齐（`1a961f8`）。
- **成果**：388 测试，lint 0 error，build 全绿。

---

## 收尾约定

- 未做项不是永久关闭：深度审计或用户反馈提供新证据时重新评估。
- 每步完成后：typecheck / lint / 相关测试 / build 全绿再进入下一步。
- P5 为架构深化，冻结新功能；达到「架构优质、基础稳定」标准后评估 1.9.0 发布。
- **1.9.0 版号会停留较久**，作为架构筑基与基础稳定的载体。
