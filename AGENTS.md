# 115m 核心项目规范

> ⚠️ **所有 AI 智能体（opencode, mimo, cursor 等）执行任何任务前必须阅读此文件！**

## 项目背景（必读）

- `115m` 是 **115 网盘的前端增强 Chrome 扩展**。
- **正式版 1.9**：稳定线，目录 `E:\qh775885\115m`，零日常在用。**仅做轻量维护与 Bug 修复，严禁在其上做大改。**
- **开发版 2.0**：支线 `feat/v2-vidstack-player`，目录 `E:\qh775885\115m-v2`，用 Vidstack 重铸播放器。
- 两个目录**物理隔离**，目的就是开发 2.0 时完全不打扰正式版使用。**动手前务必确认自己在哪个目录。**
- **当前进度**：播放器底座已更换、UI 已开发完毕；**待开发 = UI 功能实装接通**（详见 `docs/迁移计划.md`）。

## 路由机制
根据任务类型主动读取对应文档（按需查看，勿一次性全读）：
- **转码 / VIP 加速 / 115vod 接口** -> `.rules/transcode.md`（该功能多次因忽略执行环境被改坏，务必先读）
- **播放器 UI / 样式 / 新控件 / 架构 / 功能实装** -> `docs/播放器.md`（唯一专属文档：设计语言 + 四层架构 + 22 项功能落位与实装路线）
- **迭代规划、迁移进度、测试补强** -> `docs/迁移计划.md`，并按需核对 `.rules/迭代否决项.md`（已定论/已否决事项不重复提出）

## 文档索引
- `docs/播放器.md`：播放器唯一专属文档（设计语言 + 架构设计）。
- `docs/迁移计划.md`：战略阶段规划（大步骤）与播放器迁移进度（小步骤）。
- `更新日志.md` / `README.md`：发布日志与项目说明（保留根目录）。

## 全局纪律
- 改代码前先读当前文件及依赖，勿盲目瞎猜。
- 只做当前请求涉及的功能，勿顺手重构无关代码。
- 涉及数据、模块、消息、资源加载路径，务必确认绝对边界。

## 临时文件与仓库整洁（绝对边界）
- 禁止在项目根目录堆放调试笔记、会话记录、日志、一次性脚本。
- 临时产物只允许放在已 gitignore 的 `temp/`、`tmp/`、`.dbg/`（优先 `temp/`）。
- 调试结束或任务收尾时**必须删除**本任务产生的临时文件；不依赖 gitignore 长期保留。

## 功能完整性优先（绝对边界）
- **用户能感知到的现有行为，默认视为必须保留。** 实现方式可以换，行为结果不能默默变。
- 凡改动可能影响功能完整性、使用习惯或跨页面联动，**必须先说明影响、获同意后再改**，不得藏在「优化 / 重构」等提交里悄悄做。
- 典型须先告知：缩小/取消同步刷新范围、自动↔手动/单条↔批量等触发策略变化、删除兜底/降级路径、改变默认体验。

## Chrome 扩展边界
- 增加/修改 `chrome.runtime` 消息时，同步维护 `src/shared/messages.ts`。
- 修改 content/background/注入脚本时，明确执行环境（extension / page / isolated / MAIN world）。
- 涉及动态 `import()`、WXT chunk、`/assets/*` 时，优先检查相对路径。

## WXT 架构构建边界 (非常重要)
- 入口文件只在 `src/entrypoints/`，业务代码在 `src/content/`、`src/player/` 等目录。
- **AI 绝对禁止运行 `pnpm dev`**：启动/维护/停止全由用户手动管理，热重载由用户本机控制台捕获；需重启配置（如 `wxt.config.ts`）时提示用户手动重启。
- 阶段性源码修改后运行 `pnpm build` 验证语法（产物 `dist/chrome-mv3`）。
- `pnpm zip` 打包产物默认在 `.wxt/`。

## UI 与图标边界
- 侧边栏 (`src/content/core/sidebar.ts`) 使用 115 官方图标资源 (`https://115.com/icons/...`)，勿引入项目全局图标系统（Iconify/SVG sprite）。

## 安全
- 勿泄露密钥、账号、敏感信息。
- 勿删除未确认的旧版兼容代码。
