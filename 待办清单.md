# 待办清单（后续考虑）

> 记录已识别但暂不实施的项目。按优先级排列。实施前需重新评估影响面。

## 1. 无损降级记忆（用户明确暂缓）

- **目标**：某视频从无损链路降级到 115 原画后，记录"该文件不适合无损"，下次打开直接走原画，跳过无损尝试。
- **现状**：`native-playback-monitor.ts` 已能检测卡死/黑屏/静音并自动降级，但每次打开都会重新尝试无损。
- **收益**：对高码率/编码不兼容文件减少重复卡顿等待。
- **风险**：需评估判断依据（pickCode 维度？）；文件可能被重新转码/源变化后"不适合"结论过期，需考虑失效机制。
- **状态**：待启动。

## 2. 图片查看器增强

- 全屏快捷键提示、批量导出/下载当前文件夹图片、图片信息（尺寸/路径）。
- **现状**：`media-wall-images.ts` 查看器已支持缩略图条/滚轮/缩放/删除。
- **状态**：未排期。

## 3. 播放器体验增强

- 倍速记忆、播放列表记忆当前集数（跨会话续播）、字幕拖拽调节。
- **状态**：未排期。

## 4. 分享/接收页面补媒体墙

- `share_save`、`share_save_receive` 标签页目前无媒体墙增强（侧边栏已有入口但页面内无）。
- **注意**：跨页面适配，改动面大，需要单独排期与测试。
- **状态**：未排期。

## 5. 测试覆盖补强

- 媒体墙、侧边栏、解压、转码路径几乎没有测试。
- **现状**：适配层（native-dom）已有 28 个用例；播放器纯逻辑有测试。
- **状态**：持续事项，随各模块迭代逐步补充。

## 6. `参考/` 目录移出源码仓库

- 内含 115master、魔改脚本、har 文件等非项目产物，混在仓库里易误提交。
- **状态**：需用户确认后执行（涉及移动用户文件）。
