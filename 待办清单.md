# 待办清单

> 记录已识别但暂不实施的项目。按优先级排列。实施前需重新评估影响面。
> 已完成的项会清理；需要待办的才记录在此。

## 1. 抽帧链路（clipper）缺陷（中）

- **背景**：clipper 是视频封面/时间轴缩略图抽帧核心（`videoThumbnail.ts` 调用）。
- **问题**：`clipper/io/ChunkIO.ts:84-107` 服务器返回 200（不支持 Range）时直接返回数据且不推进 offset、不置 doned，`DecoderFlow._isExhausted` 依赖 isDoned（`DecoderFlow.ts:472-478`）→ 反复重读同一数据直到 5s 超时。
- **收益**：异常服务器下封面仍能生成而非超时失败。
- **状态**：待启动。

## 2. 图片查看器增强（中）

- 全屏 API（requestFullscreen）、下载/复制原图（`originalUrl` 已就绪 `media-wall-images.ts:43`）、图片信息面板（尺寸/格式/路径）、旋转、重开记忆位置（当前 `open()` 总是从 startIndex 重开 `:686`）。
- 注意：`toOriginalImageUrl` 正则 `/_\d+(\?|$)/`（`:10`）对多段尺寸 URL 会取错，改动时一并修复。
- **状态**：未排期。

## 3. 播放器体验增强（中）

- 倍速记忆（`history.ts` 无倍速存储键，`player.ts:767-779` 只改内存）。
- 播放列表续播（记忆上次播到第 N 集，当前仅单集进度恢复）。
- 字幕位置/字号/时间偏移调节（当前字幕固定渲染在 `m115-subtitle-layer`，无拖拽）。
- 快捷键可配置（当前硬编码 `keyboard.ts`）。
- **状态**：未排期。

## 4. 边界小 bug 汇总（低，随模块迭代顺手修）

- `m3u8-parser.ts:13` `url.startsWith('http')` 判断过宽，协议相对 `//cdn.xxx` 会拼成 `https://115.com//cdn...` 双斜杠。
- `archive.ts:32-34` `part011.rar`（第 11 卷）会被误判为首卷。
- `native-history.ts:61-77` `getNativeHistoryMap` 对大量 pickCode 无并发限制。
- `player-playlist-cache.ts:86-94` 读路径也触发写回，高频读频繁写 localStorage。
- `scroll-history.ts:61` 用非标准 `behavior:'instant'`。
- `home.ts:19,136` `scannedItems` WeakSet 永不清理，若 115 复用同一 li DOM 新文件会被跳过扫描。
- **状态**：未排期。

## 5. 死代码清理（低，需逐个确认）

- `lib/source.ts` `fetchUltraSource`（未使用，逻辑已在 player-services 重复实现）。
- `player/core/player-speed.ts`、`info-panel.ts`、`player-control-registry.ts`（完整抽象但零引用）。
- `subtitle-controller.ts:101-144`、`audio-manager.ts:156-164`+`player-audio.ts` 的 standalone 控件分支（当前装配不可达）。
- `background/handlers.ts` 纯桶文件（index.ts 已直接引用拆分文件）。
- `player-skin.css` 残留 `.m115-subtitle-control/.m115-audio-control/.m115-speed-control` 样式。
- `video-page.ts:8-11` `clearResourceTimings()` 清空全部性能记录（可收窄到匹配项）。
- **注意**：按项目规范"勿删除未确认的旧版兼容代码"，每项清理前需确认无引用。
- **状态**：未排期。

## 6. 测试覆盖补强（持续）

- **现状**：纯函数层覆盖较好（播放器 15 个、content 4 个、shared/lib 各 2 个）。
- **盲区**：`background/` 全部零测试；`platform/115/`、`lib/clipper/`、`lib/`（crypto/rsa/m3u8-parser/pro-api/videoThumbnail）零测试；`media-wall-images.ts`、`preview.ts`（转码状态机）、`unarchive-actions.ts`、`player.ts` 主流程零测试。
- **建议**：优先为第 1、2、4 条 bug 修复补回归测试（音轨切换、图片查看器交互、降级判定）。
- **状态**：持续事项，随各模块迭代逐步补充。

## 7. 无损降级记忆（用户明确暂缓）

- **目标**：某视频从无损链路降级到 115 原画后，记录"该文件不适合无损"，下次打开直接走原画，跳过无损尝试。
- **现状**：`native-playback-monitor.ts` 已能检测卡死/黑屏/静音并自动降级，但每次打开都会重新尝试无损。
- **收益**：对高码率/编码不兼容文件减少重复卡顿等待。
- **风险**：需评估判断依据（pickCode 维度？）；文件可能被重新转码/源变化后"不适合"结论过期，需考虑失效机制。
- **状态**：待启动。

## 8. 视频缩略图滚动抽帧限流（风控相关）

- **背景**：有用户反馈快速滚动大量视频可能触发风控。源头是快速滚动时多个视频同时进入封面抽帧，各自发 getM3u8 + 分片请求。
- **方向**：滚动抽帧限流（更激进的滚动防抖、或滚动速率高于阈值时暂停抽帧、或对未缓存视频先占位不抽帧）。
- **现状**：当前未实际遇到风控，暂不处理。若再次收到相关反馈则启动。
- **状态**：未排期。
